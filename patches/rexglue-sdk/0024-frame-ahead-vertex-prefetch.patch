diff --git a/include/rex/graphics/flags.h b/include/rex/graphics/flags.h
index 6814495..034a451 100644
--- a/include/rex/graphics/flags.h
+++ b/include/rex/graphics/flags.h
@@ -80,6 +80,7 @@ REXCVAR_DECLARE(bool, gpu_native_index);
 REXCVAR_DECLARE(bool, gpu_native_vertex);
 REXCVAR_DECLARE(bool, gpu_native_dedup);
 REXCVAR_DECLARE(bool, gpu_native_verify);
+REXCVAR_DECLARE(bool, gpu_native_prefetch);
 REXCVAR_DECLARE(uint32_t, gpu_native_hash_max_bytes);
 REXCVAR_DECLARE(uint32_t, gpu_native_hash_min_bytes);
 REXCVAR_DECLARE(bool, gpu_native_stats);
diff --git a/include/rex/perf/counter.h b/include/rex/perf/counter.h
index 4125a01..4dbf123 100644
--- a/include/rex/perf/counter.h
+++ b/include/rex/perf/counter.h
@@ -147,6 +147,12 @@ enum class CounterId : uint16_t {
   // above: this is what forces those render pass ends.
   kVkPipelineBarriers,

+  // Frame-ahead native vertex prefetch. Uploaded ranges prove the arm
+  // engaged; hits and misses report the mandatory first-draw validation.
+  kNativePrefetchRanges,
+  kNativePrefetchHits,
+  kNativePrefetchMisses,
+
   kCount  // sentinel -- must be last
 };

diff --git a/src/core/perf/counter.cpp b/src/core/perf/counter.cpp
index 26f1cdd..c229f8b 100644
--- a/src/core/perf/counter.cpp
+++ b/src/core/perf/counter.cpp
@@ -68,6 +68,9 @@ constexpr const char* kCounterNames[] = {
     "memexport_unresolved",
     "render_pass_begins",
     "vk_pipeline_barriers",
+    "native_prefetch_ranges",
+    "native_prefetch_hits",
+    "native_prefetch_misses",
 };
 static_assert(std::size(kCounterNames) == kNumCounters, "kCounterNames must match CounterId enum");

@@ -110,6 +113,9 @@ constexpr bool kIsGauge[] = {
     false,  // kMemexportUnresolved
     false,  // kRenderPassBegins
     false,  // kVkPipelineBarriers
+    false,  // kNativePrefetchRanges
+    false,  // kNativePrefetchHits
+    false,  // kNativePrefetchMisses
 };
 static_assert(std::size(kIsGauge) == kNumCounters, "kIsGauge must match CounterId enum");

diff --git a/src/graphics/flags.cpp b/src/graphics/flags.cpp
index 44cbe65..3ebfaf3 100644
--- a/src/graphics/flags.cpp
+++ b/src/graphics/flags.cpp
@@ -70,6 +70,11 @@ REXCVAR_DEFINE_BOOL(gpu_native_verify, false, "GPU",
                     "Re-hash native ranges on a dedup hit and warn plus re-upload on a "
                     "mismatch (intra-frame rewrite oracle, expensive)")
     .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_prefetch, false, "GPU",
+                    "EXPERIMENTAL - upload last frame's vertex working set at the next swap "
+                    "so transfer barriers land before the render pass. Every range is "
+                    "fingerprinted again before its first draw.")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
 REXCVAR_DEFINE_UINT32(gpu_native_hash_max_bytes, 16777216, "GPU",
                       "Ranges larger than this take the emulated path instead of being "
                       "hashed and copied")
diff --git a/src/graphics/vulkan/command_processor.cpp b/src/graphics/vulkan/command_processor.cpp
index 41eae62..b8dc4d0 100644
--- a/src/graphics/vulkan/command_processor.cpp
+++ b/src/graphics/vulkan/command_processor.cpp
@@ -2341,6 +2341,7 @@ void VulkanCommandProcessor::IssueSwap(uint32_t frontbuffer_ptr, uint32_t frontb
     REXGPU_ERROR("XELOG_GPU PRESENT: BeginSubmission FAILED");
     return;
   }
+  native_residency_->PrefetchFrame(*memory_, *shared_memory_);

   bool skip_present_due_async_placeholder = REXCVAR_GET(async_shader_compilation) &&
                                             REXCVAR_GET(vulkan_async_skip_incomplete_frames) &&
@@ -4110,10 +4111,14 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       if (REXCVAR_GET(gpu_native_stats)) {
         native_telemetry::NoteVertexRange(vfetch_address_bytes, vfetch_size_bytes);
       }
+      if (native_residency_->IsVertexResidencyAllowedForDraw()) {
+        native_residency_->NoteVertexRangeForNextFrame(vfetch_address_bytes, vfetch_size_bytes);
+      }
       VertexBufferState& state = vertex_buffer_states_[vfetch_index];
       vfetch_cached |=
           state.address == vfetch_constant.address && state.size == vfetch_constant.size;
-      if (vfetch_cached && !native_residency_->VerifyBypassesCallerCache()) {
+      if (vfetch_cached && !native_residency_->VerifyBypassesCallerCache() &&
+          !native_residency_->RequiresPrefetchValidation(vfetch_address_bytes, vfetch_size_bytes)) {
         // The fetch constant has not moved since this buffer was last made
         // resident, so neither path has anything to do - which is also why the
         // verify oracle has to look past this: an in-place rewrite of a buffer
diff --git a/src/graphics/vulkan/native_residency.cc b/src/graphics/vulkan/native_residency.cc
index cce02b0..5ba875d 100644
--- a/src/graphics/vulkan/native_residency.cc
+++ b/src/graphics/vulkan/native_residency.cc
@@ -62,6 +62,10 @@ void NativeResidency::LatchFrameModeAtSwap() {
   // Only ever true in a native frame: the oracle judges this path, and the
   // emulated path must not change shape because a diagnostic cvar is set.
   verify_frame_ = native_frame_ && REXCVAR_GET(gpu_native_verify);
+  // Prefetch is a scheduling experiment on the normal deduplicated path. It
+  // stays out of both diagnostic modes so their meaning does not change.
+  prefetch_frame_ =
+      native_vertex_frame_ && dedup_frame_ && !verify_frame_ && REXCVAR_GET(gpu_native_prefetch);
   hash_max_bytes_frame_ = REXCVAR_GET(gpu_native_hash_max_bytes);
   // The oracle exists to compare fingerprints against the guest bytes, so a
   // verify frame takes one for every range whatever the threshold says. That
@@ -87,6 +91,90 @@ void NativeResidency::LatchFrameModeAtSwap() {
                                                                        : std::next(it);
     }
   }
+
+  if (prefetch_frame_) {
+    prefetch_ranges_ = std::move(current_frame_ranges_);
+    current_frame_ranges_.clear();
+  } else {
+    current_frame_ranges_.clear();
+    prefetch_ranges_.clear();
+  }
+  current_frame_range_keys_.clear();
+}
+
+void NativeResidency::PrefetchFrame(memory::Memory& memory, SharedMemory& shared_memory) {
+  if (!prefetch_frame_ || prefetch_ranges_.empty()) {
+    return;
+  }
+
+  struct UploadedRange {
+    uint64_t key;
+    uint32_t start;
+    uint32_t length;
+    uint64_t hash;
+  };
+  std::vector<UploadedRange> uploaded;
+  uploaded.reserve(prefetch_ranges_.size());
+
+  // Uploads still use the existing proven copy path. Their transfer barriers
+  // are individually conservative for this first experiment, but they all
+  // execute here while the outgoing pass is ending, rather than interleaved
+  // with the next frame's draws. This isolates pass scheduling before barrier
+  // coalescing changes any other variable.
+  for (const VertexRange& range : prefetch_ranges_) {
+    if (!range.length || range.start >= SharedMemory::kBufferSize ||
+        SharedMemory::kBufferSize - range.start < range.length ||
+        range.length > hash_max_bytes_frame_ ||
+        IntersectsResolveDestination(range.start, range.length)) {
+      continue;
+    }
+    const uint64_t key = RangeKey(range.start, range.length);
+    auto existing = range_table_.find(key);
+    if (existing != range_table_.end() && existing->second.blacklisted) {
+      continue;
+    }
+
+    const void* source = memory.TranslatePhysical<const void*>(range.start);
+    PERF_counter_add(kNativeBytesHashed, range.length);
+    const uint64_t hash = XXH3_64bits(source, range.length);
+    if (!shared_memory.UploadRangeUnwatched(range.start, range.length, source)) {
+      range_table_.erase(key);
+      continue;
+    }
+    uploaded.push_back({key, range.start, range.length, hash});
+    PERF_counter_inc(kNativePrefetchRanges);
+  }
+
+  // Stamp entries only after every copy. Overlapping vertex windows contain
+  // the same guest bytes in their overlap, and MaxWriteSeq now records the
+  // final batch state rather than making an earlier copy look clobbered by a
+  // later one from this same prefetch wave.
+  for (const UploadedRange& range : uploaded) {
+    Entry& entry =
+        range_table_.try_emplace(range.key, Entry{0, false, 0, 0, false, false}).first->second;
+    entry.hash = range.hash;
+    entry.hash_valid = true;
+    entry.last_epoch = frame_epoch_;
+    entry.upload_seq = shared_memory.MaxWriteSeq(range.start, range.length);
+    entry.prefetched = true;
+  }
+  prefetch_ranges_.clear();
+}
+
+void NativeResidency::NoteVertexRangeForNextFrame(uint32_t start, uint32_t length) {
+  if (!prefetch_frame_ || !length || start >= SharedMemory::kBufferSize ||
+      SharedMemory::kBufferSize - start < length) {
+    return;
+  }
+  const uint64_t key = RangeKey(start, length);
+  if (current_frame_range_keys_.insert(key).second) {
+    current_frame_ranges_.push_back({start, length});
+  }
+}
+
+bool NativeResidency::RequiresPrefetchValidation(uint32_t start, uint32_t length) const {
+  auto it = range_table_.find(RangeKey(start, length));
+  return it != range_table_.end() && it->second.prefetched;
 }

 void NativeResidency::BeginDraw(bool allowed) {
@@ -161,6 +249,7 @@ bool NativeResidency::UploadRange(memory::Memory& memory, SharedMemory& shared_m
   // What guest memory held (hash) and what the buffer held (upload_seq) at the
   // same instant. A hit needs both to still be true.
   entry.upload_seq = upload_seq;
+  entry.prefetched = false;
   return true;
 }

@@ -200,11 +289,11 @@ bool NativeResidency::RequestRangeNative(memory::Memory& memory, SharedMemory& s
     // is recorded either, so that turning dedup back on starts from a clean
     // table rather than from entries that were never maintained.
     range_table_.erase(key);
-    Entry scratch = {0, false, 0, 0, false};
+    Entry scratch = {0, false, 0, 0, false, false};
     return UploadRange(memory, shared_memory, start, length, key, scratch, 0, false);
   }

-  auto [it, inserted] = range_table_.try_emplace(key, Entry{0, false, 0, 0, false});
+  auto [it, inserted] = range_table_.try_emplace(key, Entry{0, false, 0, 0, false, false});
   Entry& entry = it->second;
   if (inserted || entry.blacklisted) {
     // Never seen, or seen and caught lying by the verify oracle.
@@ -232,6 +321,22 @@ bool NativeResidency::RequestRangeNative(memory::Memory& memory, SharedMemory& s
     PERF_counter_inc(kNativeSeqEvictions);
   }

+  if (entry.prefetched) {
+    // A frame-boundary snapshot is only a prediction. The guest may generate
+    // or rewrite dynamic vertices between swap and draw, so the first consumer
+    // must prove the bytes are unchanged even when the caller's fetch constant
+    // cache says the range is resident.
+    entry.prefetched = false;
+    PERF_counter_add(kNativeBytesHashed, length);
+    const uint64_t hash = XXH3_64bits(memory.TranslatePhysical<const void*>(start), length);
+    if (!clobbered && entry.hash_valid && hash == entry.hash) {
+      PERF_counter_inc(kNativePrefetchHits);
+      return true;
+    }
+    PERF_counter_inc(kNativePrefetchMisses);
+    return UploadRange(memory, shared_memory, start, length, key, entry, hash, true);
+  }
+
   if (!clobbered && entry.last_epoch == frame_epoch_) {
     // Already made resident this frame - the common case, a vertex buffer
     // shared by many draws of one frame. Free unless the oracle is on.
@@ -350,9 +455,10 @@ bool NativeResidency::MayFingerprintConversionSource(const void* guest_source, u
     // is reading them is caught by the next reuse instead of being baked into
     // the fingerprint.
     PERF_counter_add(kNativeBytesHashed, length);
-    Entry& entry = (it != range_table_.end())
-                       ? it->second
-                       : range_table_.try_emplace(key, Entry{0, false, 0, 0, false}).first->second;
+    Entry& entry =
+        (it != range_table_.end())
+            ? it->second
+            : range_table_.try_emplace(key, Entry{0, false, 0, 0, false, false}).first->second;
     entry.hash = XXH3_64bits(guest_source, length);
     // Conversion sources are outside gpu_native_hash_min_bytes: there is
     // nothing to copy for one, so the fingerprint's alternative is re-arming
diff --git a/src/graphics/vulkan/native_residency.h b/src/graphics/vulkan/native_residency.h
index d11ec9d..6d617d7 100644
--- a/src/graphics/vulkan/native_residency.h
+++ b/src/graphics/vulkan/native_residency.h
@@ -13,6 +13,7 @@

 #include <cstdint>
 #include <unordered_map>
+#include <unordered_set>
 #include <vector>

 #include <rex/graphics/shared_memory.h>
@@ -52,6 +53,11 @@ class NativeResidency {
   // and where stale fingerprint entries are swept.
   void LatchFrameModeAtSwap();

+  // Copies the vertex working set observed in the previous frame before the
+  // next frame enters its render pass. LatchFrameModeAtSwap prepares the set;
+  // IssueSwap calls this only after BeginSubmission has made recording safe.
+  void PrefetchFrame(memory::Memory& memory, SharedMemory& shared_memory);
+
   bool native_frame() const { return native_frame_; }
   // Frames since startup. The fingerprint cache stamps its entries with this
   // to tell "already made resident this frame" from "seen in an older frame".
@@ -108,6 +114,15 @@ class NativeResidency {
   bool RequestRangeNative(memory::Memory& memory, SharedMemory& shared_memory, uint32_t start,
                           uint32_t length, bool caller_cache_hit = false);

+  // Called for every referenced vertex range before the command processor's
+  // fetch-constant cache can early out. The next frame can therefore prefetch
+  // the full working set, including ranges whose fetch constants never move.
+  void NoteVertexRangeForNextFrame(uint32_t start, uint32_t length);
+
+  // A prefetched range must bypass the fetch-constant cache once so its guest
+  // fingerprint is checked at the first draw after the frame-boundary copy.
+  bool RequiresPrefetchValidation(uint32_t start, uint32_t length) const;
+
   // The converted index cache's half of the same policy. That cache watches
   // the guest indices it converted from so that a conversion reused later in
   // the frame is dropped if the guest has rewritten them, and re-arms the
@@ -184,6 +199,9 @@ class NativeResidency {
     // source entries, which are read out of guest memory and never copied into
     // the buffer at all.
     uint64_t upload_seq;
+    // Set by frame-ahead prefetch and cleared by the first draw that validates
+    // the guest fingerprint. A prefetch is never trusted merely by epoch.
+    bool prefetched;
     // Set by the verify oracle when the fingerprint was proven wrong for this
     // range: it is then copied on every request, with no dedup, for the rest
     // of the run.
@@ -218,6 +236,7 @@ class NativeResidency {
   bool native_vertex_frame_ = false;
   bool dedup_frame_ = true;
   bool verify_frame_ = false;
+  bool prefetch_frame_ = false;
   uint32_t hash_max_bytes_frame_ = 0;
   // Latched like the rest, and forced to 0 whenever verify_frame_ is set so
   // that the oracle never grades a frame that skipped its fingerprints.
@@ -244,6 +263,14 @@ class NativeResidency {
   // every hit path in RequestRangeNative.
   std::unordered_map<uint64_t, Entry> range_table_;

+  struct VertexRange {
+    uint32_t start;
+    uint32_t length;
+  };
+  std::vector<VertexRange> current_frame_ranges_;
+  std::unordered_set<uint64_t> current_frame_range_keys_;
+  std::vector<VertexRange> prefetch_ranges_;
+
   // One bit per 4 KiB page of the shared memory buffer, set for every page any
   // resolve has ever written. 16 KiB, allocated once.
   std::vector<uint64_t> resolve_pages_;

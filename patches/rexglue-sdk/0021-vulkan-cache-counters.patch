diff --git a/src/graphics/vulkan/pipeline_cache.cpp b/src/graphics/vulkan/pipeline_cache.cpp
index 86edb1e..306913d 100644
--- a/src/graphics/vulkan/pipeline_cache.cpp
+++ b/src/graphics/vulkan/pipeline_cache.cpp
@@ -23,6 +23,7 @@
 
 #include <fmt/format.h>
 
+#include <rex/perf/counter.h>
 #include <rex/assert.h>
 #include <rex/cvar.h>
 #include <rex/dbg.h>
@@ -1127,6 +1128,7 @@ bool VulkanPipelineCache::ConfigurePipeline(
 
   auto it = pipelines_.find(description);
   if (it != pipelines_.end()) {
+    PROFILE_PIPELINE_CACHE_HIT();
     VkPipeline found_pipeline = it->second.pipeline.load(std::memory_order_acquire);
     if (found_pipeline == VK_NULL_HANDLE) {
       PipelineCreationArguments creation_arguments;
@@ -1147,7 +1149,14 @@ bool VulkanPipelineCache::ConfigurePipeline(
     return pipeline_out != VK_NULL_HANDLE && pipeline_layout_out != nullptr;
   }
 
-  // Create the pipeline if not already existing.
+  // Create the pipeline if not already existing. This is the shader-compile
+  // hitch signal: a miss here means a pipeline has to be built before the draw
+  // can proceed (or a placeholder used and the frame dropped, if async
+  // compilation is on). The counter existed but had call sites only in the
+  // D3D12 backend, so on Vulkan - the backend this project ships - it read a
+  // constant zero, while perf_summary.py described it as exactly this signal.
+  PROFILE_PIPELINE_CACHE_MISS();
+
   auto& pipeline = *pipelines_.emplace(description, Pipeline()).first;
   PipelineCreationArguments creation_arguments_real;
   if (!TryGetPipelineCreationArgumentsForDescription(description, &pipeline,
diff --git a/src/graphics/vulkan/texture_cache.cpp b/src/graphics/vulkan/texture_cache.cpp
index fc46bf8..76802e9 100644
--- a/src/graphics/vulkan/texture_cache.cpp
+++ b/src/graphics/vulkan/texture_cache.cpp
@@ -15,6 +15,7 @@
 #include <cstring>
 #include <utility>
 
+#include <rex/perf/counter.h>
 #include <rex/assert.h>
 #include <rex/bit.h>
 #include <rex/logging.h>
@@ -1895,8 +1896,10 @@ VkImageView VulkanTextureCache::VulkanTexture::GetView(bool is_signed, uint32_t
   // Try to find an existing view.
   auto it = views_.find(view_key);
   if (it != views_.end()) {
+    PROFILE_TEXTURE_CACHE_HIT();
     return it->second;
   }
+  PROFILE_TEXTURE_CACHE_MISS();
 
   // Create a new view.
   const ui::vulkan::VulkanDevice::Functions& dfn = vulkan_device->functions();
diff --git a/src/graphics/vulkan/command_processor.cpp b/src/graphics/vulkan/command_processor.cpp
index 89c4eb6..181b08a 100644
--- a/src/graphics/vulkan/command_processor.cpp
+++ b/src/graphics/vulkan/command_processor.cpp
@@ -5129,6 +5129,11 @@ void VulkanCommandProcessor::CheckSubmissionFenceAndDeviceLoss(uint64_t await_su
     // defined by vkQueueSubmit additionally include in the first
     // synchronization scope all commands that occur earlier in submission
     // order."
+    // A genuine stall: this blocks with no timeout until the GPU catches up.
+    // (The loop below waits with a timeout of 0, which is a poll, not a stall,
+    // and is deliberately not counted.) PROFILE_CMD_BUFFER_STALL had no call
+    // site anywhere in the tree until now.
+    PROFILE_CMD_BUFFER_STALL();
     VkResult wait_result =
         dfn.vkWaitForFences(device, uint32_t(await_submission - submission_completed_),
                             submissions_in_flight_fences_.data(), VK_TRUE, UINT64_MAX);

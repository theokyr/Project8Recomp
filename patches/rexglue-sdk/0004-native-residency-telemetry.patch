diff --git a/include/rex/graphics/flags.h b/include/rex/graphics/flags.h
index c72380a..a91ffd1 100644
--- a/include/rex/graphics/flags.h
+++ b/include/rex/graphics/flags.h
@@ -70,6 +70,19 @@ bool IsGpuDebugMarkersEnabled();
 // GPU Alpha Test
 REXCVAR_DECLARE(bool, use_fuzzy_alpha_epsilon);
 
+// GPU Native Residency
+// Policy switches for the native-residency draw path and its telemetry. Every
+// one of them defaults to the emulated behavior, so a build with this header
+// behaves exactly like a build without it until a cvar is flipped.
+REXCVAR_DECLARE(bool, gpu_native_residency);
+REXCVAR_DECLARE(bool, gpu_native_index);
+REXCVAR_DECLARE(bool, gpu_native_vertex);
+REXCVAR_DECLARE(bool, gpu_native_dedup);
+REXCVAR_DECLARE(bool, gpu_native_verify);
+REXCVAR_DECLARE(uint32_t, gpu_native_hash_max_bytes);
+REXCVAR_DECLARE(bool, gpu_native_stats);
+REXCVAR_DECLARE(bool, gpu_native_suppress_draws);
+
 // GPU Shader Translation / Tracing
 REXCVAR_DECLARE(std::string, dump_shaders);
 REXCVAR_DECLARE(std::string, trace_gpu_prefix);
@@ -109,3 +122,27 @@ REXCVAR_DECLARE(bool, d3d12_readback_resolve);
 #endif  // REX_HAS_D3D12
 
 #define XE_GPU_FINE_GRAINED_DRAW_SCOPES 1
+
+namespace rex::graphics::native_telemetry {
+
+// Diagnostic bookkeeping for the native-residency work: it answers "how much
+// unique vertex data does a frame actually reference", "how much of the
+// RequestRange traffic is vertex versus texture", and "does any resolve ever
+// write a range a vertex fetch later reads".
+//
+// Every entry point here is called only from the command-processor thread and
+// only from inside an `if (REXCVAR_GET(gpu_native_stats))` at the call site,
+// so with the cvar at its default the cost is a single predictable branch and
+// the state below is never touched. Nothing here is synchronized.
+void NoteVertexRange(uint32_t start, uint32_t length);
+void NoteIndexRange(uint32_t start, uint32_t length);
+void NoteTextureRange(uint32_t start, uint32_t length);
+void NoteResolveDestination(uint32_t start, uint32_t length);
+
+// Publishes the per-frame derived counters (unique vertex bytes, shared page
+// overlaps) and emits the ~1 Hz summary line. Called from the XE_SWAP handler
+// once the swap has been issued. `cp_drain_us` is the freshly measured drain
+// for the frame that just ended, or -1 when it could not be measured.
+void EndFrame(int64_t cp_drain_us);
+
+}  // namespace rex::graphics::native_telemetry
diff --git a/include/rex/perf/counter.h b/include/rex/perf/counter.h
index 167b645..803f335 100644
--- a/include/rex/perf/counter.h
+++ b/include/rex/perf/counter.h
@@ -49,6 +49,23 @@ enum class CounterId : uint16_t {
   kPipelineCacheHits,
   kPipelineCacheMisses,
 
+  // Native residency telemetry.
+  // Appended (never reordered) so the frames.csv column set only ever grows.
+  // Everything here is written from the command-processor thread; the ones
+  // that cost more than a relaxed atomic add are gated on `gpu_native_stats`.
+  kNativeDraws,
+  kNativeFallbackDraws,
+  kNativeBytesHashed,
+  kNativeBytesUploaded,
+  kNativeUniqueVertexBytes,
+  kNativeIndexBytes,
+  kMakeRangeValidCalls,
+  kWatchRearms,
+  kRequestRangeVertexBytes,
+  kRequestRangeTextureBytes,
+  kSharedPageOverlaps,
+  kCpDrainUs,
+
   kCount  // sentinel -- must be last
 };
 
diff --git a/src/core/perf/counter.cpp b/src/core/perf/counter.cpp
index 536009e..455c34a 100644
--- a/src/core/perf/counter.cpp
+++ b/src/core/perf/counter.cpp
@@ -49,6 +49,18 @@ constexpr const char* kCounterNames[] = {
     "texture_cache_misses",
     "pipeline_cache_hits",
     "pipeline_cache_misses",
+    "native_draws",
+    "native_fallback_draws",
+    "native_bytes_hashed",
+    "native_bytes_uploaded",
+    "native_unique_vertex_bytes",
+    "native_index_bytes",
+    "make_range_valid_calls",
+    "watch_rearms",
+    "request_range_vertex_bytes",
+    "request_range_texture_bytes",
+    "shared_page_overlaps",
+    "cp_drain_us",
 };
 static_assert(std::size(kCounterNames) == kNumCounters, "kCounterNames must match CounterId enum");
 
@@ -72,6 +84,18 @@ constexpr bool kIsGauge[] = {
     false,  // kTextureCacheMisses
     false,  // kPipelineCacheHits
     false,  // kPipelineCacheMisses
+    false,  // kNativeDraws
+    false,  // kNativeFallbackDraws
+    false,  // kNativeBytesHashed
+    false,  // kNativeBytesUploaded
+    false,  // kNativeUniqueVertexBytes   (set once per frame)
+    false,  // kNativeIndexBytes
+    false,  // kMakeRangeValidCalls
+    false,  // kWatchRearms
+    false,  // kRequestRangeVertexBytes
+    false,  // kRequestRangeTextureBytes
+    false,  // kSharedPageOverlaps        (set once per frame)
+    false,  // kCpDrainUs                 (set once per frame)
 };
 static_assert(std::size(kIsGauge) == kNumCounters, "kIsGauge must match CounterId enum");
 
diff --git a/src/graphics/command_processor.cpp b/src/graphics/command_processor.cpp
index 60efcd3..ca3e9a7 100644
--- a/src/graphics/command_processor.cpp
+++ b/src/graphics/command_processor.cpp
@@ -86,6 +86,14 @@ using namespace rex::graphics::xenos;
 
 namespace {
 
+// Host tick at which the last draw packet of the current frame finished being
+// issued, or 0 when nothing has been drawn since the previous swap. Only ever
+// touched from the command processor thread, and only while gpu_native_stats
+// is set. The delta from here to the end of swap handling is the command
+// processor drain - the part of the frame the guest's swap-complete callback
+// is waiting on.
+uint64_t g_last_draw_tick = 0;
+
 ReadbackResolveMode ParseReadbackResolveMode(std::string_view value) {
   if (value == "fast") {
     return ReadbackResolveMode::kFast;
@@ -1098,6 +1106,23 @@ bool CommandProcessor::ExecutePacketType3_XE_SWAP(memory::RingBuffer* reader, ui
 
   IssueSwap(frontbuffer_ptr, frontbuffer_width, frontbuffer_height);
 
+  // Native residency telemetry, strictly after the swap has been issued so the
+  // guest-visible swap tail keeps its exact shape. Profiler::Flip() above has
+  // already snapshotted and zeroed the frame's counters, so the value set here
+  // lands in the *next* CSV row - the row after the one holding the draws it
+  // belongs to.
+  if (REXCVAR_GET(gpu_native_stats)) {
+    int64_t drain_us = -1;
+    if (g_last_draw_tick) {
+      uint64_t now = rex::chrono::Clock::QueryHostTickCount();
+      uint64_t frequency = rex::chrono::Clock::QueryHostTickFrequency();
+      drain_us = static_cast<int64_t>((now - g_last_draw_tick) * 1000000 / frequency);
+      PERF_counter_set(kCpDrainUs, drain_us);
+      g_last_draw_tick = 0;
+    }
+    native_telemetry::EndFrame(drain_us);
+  }
+
   ++counter_;
   return true;
 }
@@ -1541,6 +1566,10 @@ bool CommandProcessor::ExecutePacketType3Draw(memory::RingBuffer* reader, uint32
     }
   }
 
+  if (REXCVAR_GET(gpu_native_stats)) {
+    g_last_draw_tick = rex::chrono::Clock::QueryHostTickCount();
+  }
+
   // If read the packed correctly, but merely couldn't execute it (because of,
   // for instance, features not supported by the host), don't terminate command
   // buffer processing as that would leave rendering in a way more inconsistent
diff --git a/src/graphics/flags.cpp b/src/graphics/flags.cpp
index e2983a4..2e74970 100644
--- a/src/graphics/flags.cpp
+++ b/src/graphics/flags.cpp
@@ -11,8 +11,17 @@
 
 #include <rex/graphics/flags.h>
 #include <rex/logging.h>
+#include <rex/perf/counter.h>
 #include <rex/ui/renderdoc_api.h>
 
+#include <rex/chrono/clock.h>
+#include <rex/math.h>
+
+#include <algorithm>
+#include <cstdint>
+#include <unordered_set>
+#include <vector>
+
 REXCVAR_DEFINE_BOOL(gpu_allow_invalid_fetch_constants, false, "GPU",
                     "Allow invalid fetch constants");
 REXCVAR_DEFINE_BOOL(native_2x_msaa, true, "GPU", "Enable native 2x MSAA");
@@ -33,6 +42,42 @@ REXCVAR_DEFINE_BOOL(gpu_debug_markers, false, "GPU",
                     "like PIX and RenderDoc. Automatically enabled when "
                     "RenderDoc is detected.");
 
+// Native residency. The master switch and every subsystem switch default to
+// the emulated path, so an unconfigured run is bit-identical to a build
+// without this patch.
+REXCVAR_DEFINE_BOOL(gpu_native_residency, false, "GPU",
+                    "Make draw vertex/index data resident by copying it from guest memory "
+                    "instead of page-watching it. Latched once per frame at swap.")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_index, true, "GPU",
+                    "Use the native residency path for index buffers (requires "
+                    "gpu_native_residency)")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_vertex, true, "GPU",
+                    "Use the native residency path for vertex buffers (requires "
+                    "gpu_native_residency)")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_dedup, true, "GPU",
+                    "Skip native uploads whose range fingerprint is unchanged since the last "
+                    "frame that referenced it (off = hash and copy on every request)")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_verify, false, "GPU",
+                    "Re-hash native ranges on a dedup hit and warn plus re-upload on a "
+                    "mismatch (intra-frame rewrite oracle, expensive)")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_UINT32(gpu_native_hash_max_bytes, 16777216, "GPU",
+                      "Ranges larger than this take the emulated path instead of being "
+                      "hashed and copied")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_stats, false, "GPU",
+                    "Collect and log native-residency telemetry: per-frame CP drain, "
+                    "unique vertex bytes, shared page overlaps, resolve destinations")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(gpu_native_suppress_draws, false, "GPU",
+                    "Diagnostic only: drop every draw at the top of IssueDraw to measure the "
+                    "frame rate ceiling of the command processor. Output is garbage.")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+
 bool IsGpuDebugMarkersEnabled() {
   static bool cached = false;
   static bool result = false;
@@ -51,3 +96,231 @@ bool IsGpuDebugMarkersEnabled() {
   }
   return result;
 }
+
+namespace rex::graphics::native_telemetry {
+
+namespace {
+
+// The shared memory buffer the ranges below index into is 512 MiB of guest
+// physical memory (SharedMemory::kBufferSize); page granularity for the
+// overlap test is the 4 KiB host page.
+constexpr uint32_t kBufferSize = 512u * 1024u * 1024u;
+constexpr uint32_t kPageSizeLog2 = 12;
+constexpr uint32_t kPageCount = kBufferSize >> kPageSizeLog2;
+constexpr size_t kPageWords = kPageCount / 64;
+
+struct State {
+  // Per-frame: which distinct (start, length) ranges a vertex fetch asked for,
+  // and the pages those ranges and the frame's texture requests cover.
+  std::unordered_set<uint64_t> vertex_ranges;
+  std::vector<uint64_t> vertex_pages = std::vector<uint64_t>(kPageWords, 0);
+  std::vector<uint64_t> texture_pages = std::vector<uint64_t>(kPageWords, 0);
+  int64_t unique_vertex_bytes = 0;
+
+  // Session lifetime: every distinct resolve destination seen so far, and how
+  // many of them landed on a page some vertex fetch also read. The design
+  // routes any such range to the emulated path unconditionally; this is the
+  // measurement that says whether that guard is ever hot.
+  std::unordered_set<uint64_t> resolve_ranges;
+  std::vector<uint64_t> vertex_pages_session = std::vector<uint64_t>(kPageWords, 0);
+  uint64_t resolve_vertex_overlaps = 0;
+
+  // ~1 Hz summary accumulation.
+  uint64_t log_tick = 0;
+  uint64_t frames = 0;
+  int64_t draws = 0;
+  int64_t fallback_draws = 0;
+  int64_t make_range_valid_calls = 0;
+  int64_t watch_rearms = 0;
+  int64_t request_range_vertex_bytes = 0;
+  int64_t request_range_texture_bytes = 0;
+  int64_t unique_vertex_bytes_sum = 0;
+  int64_t index_bytes = 0;
+  int64_t shared_page_overlaps = 0;
+  int64_t drain_us_sum = 0;
+  int64_t drain_us_max = 0;
+  uint64_t drain_samples = 0;
+};
+
+State& state() {
+  static State s;
+  return s;
+}
+
+// Returns false when the range is empty or falls outside the shared memory
+// buffer, in which case there is nothing meaningful to record.
+bool ClampRange(uint32_t& start, uint32_t& length) {
+  if (!length || start >= kBufferSize) {
+    return false;
+  }
+  length = std::min(length, kBufferSize - start);
+  return true;
+}
+
+void MarkPages(std::vector<uint64_t>& pages, uint32_t start, uint32_t length) {
+  uint32_t page_first = start >> kPageSizeLog2;
+  uint32_t page_last = (start + length - 1) >> kPageSizeLog2;
+  for (uint32_t page = page_first; page <= page_last; ++page) {
+    pages[page >> 6] |= uint64_t(1) << (page & 63);
+  }
+}
+
+bool AnyPageSet(const std::vector<uint64_t>& pages, uint32_t start, uint32_t length) {
+  uint32_t page_first = start >> kPageSizeLog2;
+  uint32_t page_last = (start + length - 1) >> kPageSizeLog2;
+  for (uint32_t page = page_first; page <= page_last; ++page) {
+    if (pages[page >> 6] & (uint64_t(1) << (page & 63))) {
+      return true;
+    }
+  }
+  return false;
+}
+
+constexpr uint64_t RangeKey(uint32_t start, uint32_t length) {
+  return uint64_t(start) | (uint64_t(length) << 32);
+}
+
+}  // namespace
+
+void NoteVertexRange(uint32_t start, uint32_t length) {
+  if (!ClampRange(start, length)) {
+    return;
+  }
+  State& s = state();
+  if (!s.vertex_ranges.insert(RangeKey(start, length)).second) {
+    return;
+  }
+  s.unique_vertex_bytes += length;
+  MarkPages(s.vertex_pages, start, length);
+  MarkPages(s.vertex_pages_session, start, length);
+}
+
+void NoteIndexRange(uint32_t start, uint32_t length) {
+  if (!ClampRange(start, length)) {
+    return;
+  }
+  // Index data shares the vertex page set: an index buffer sitting on a
+  // watched texture page is the same blind spot as a vertex buffer doing so.
+  State& s = state();
+  MarkPages(s.vertex_pages, start, length);
+  MarkPages(s.vertex_pages_session, start, length);
+}
+
+void NoteTextureRange(uint32_t start, uint32_t length) {
+  if (!ClampRange(start, length)) {
+    return;
+  }
+  MarkPages(state().texture_pages, start, length);
+}
+
+void NoteResolveDestination(uint32_t start, uint32_t length) {
+  if (!ClampRange(start, length)) {
+    return;
+  }
+  State& s = state();
+  if (!s.resolve_ranges.insert(RangeKey(start, length)).second) {
+    return;
+  }
+  bool hits_vertex_data = AnyPageSet(s.vertex_pages_session, start, length);
+  if (hits_vertex_data) {
+    ++s.resolve_vertex_overlaps;
+  }
+  REXLOG_INFO(
+      "gpu_native_stats: resolve destination 0x{:08X} + 0x{:X} bytes ({} unique so far, "
+      "vertex-range overlap: {})",
+      start, length, s.resolve_ranges.size(), hits_vertex_data ? "YES" : "no");
+}
+
+void EndFrame(int64_t cp_drain_us) {
+  State& s = state();
+
+  uint32_t overlap_pages = 0;
+  for (size_t i = 0; i < kPageWords; ++i) {
+    overlap_pages += static_cast<uint32_t>(
+        rex::bit_count(s.vertex_pages[i] & s.texture_pages[i]));
+  }
+
+  PERF_counter_set(kNativeUniqueVertexBytes, s.unique_vertex_bytes);
+  PERF_counter_set(kSharedPageOverlaps, static_cast<int64_t>(overlap_pages));
+
+  s.vertex_ranges.clear();
+  std::fill(s.vertex_pages.begin(), s.vertex_pages.end(), uint64_t(0));
+  std::fill(s.texture_pages.begin(), s.texture_pages.end(), uint64_t(0));
+  int64_t unique_vertex_bytes = s.unique_vertex_bytes;
+  s.unique_vertex_bytes = 0;
+
+  // The frame's accumulators were snapshotted by Profiler::Flip() at the top
+  // of the swap packet, so the snapshot buffer is exactly the frame that just
+  // ended. The two values computed above were set after that snapshot and are
+  // therefore carried in locally rather than read back.
+  using rex::perf::CounterId;
+  using rex::perf::GetSnapshotCounter;
+  ++s.frames;
+  s.draws += GetSnapshotCounter(CounterId::kDrawCalls);
+  s.fallback_draws += GetSnapshotCounter(CounterId::kNativeFallbackDraws);
+  s.make_range_valid_calls += GetSnapshotCounter(CounterId::kMakeRangeValidCalls);
+  s.watch_rearms += GetSnapshotCounter(CounterId::kWatchRearms);
+  s.request_range_vertex_bytes += GetSnapshotCounter(CounterId::kRequestRangeVertexBytes);
+  s.request_range_texture_bytes += GetSnapshotCounter(CounterId::kRequestRangeTextureBytes);
+  s.index_bytes += GetSnapshotCounter(CounterId::kNativeIndexBytes);
+  s.unique_vertex_bytes_sum += unique_vertex_bytes;
+  s.shared_page_overlaps += static_cast<int64_t>(overlap_pages);
+  if (cp_drain_us >= 0) {
+    s.drain_us_sum += cp_drain_us;
+    s.drain_us_max = std::max(s.drain_us_max, cp_drain_us);
+    ++s.drain_samples;
+  }
+
+  uint64_t now = rex::chrono::Clock::QueryHostTickCount();
+  uint64_t frequency = rex::chrono::Clock::QueryHostTickFrequency();
+  if (!s.log_tick) {
+    s.log_tick = now;
+    return;
+  }
+  if (now - s.log_tick < frequency) {
+    return;
+  }
+  s.log_tick = now;
+
+  // Two halves: per-frame averages for a human reading the log, then the raw
+  // window totals as key=value so post-processing does not have to unpick
+  // rounded prose.
+  double frames = static_cast<double>(s.frames);
+  REXLOG_INFO(
+      "gpu_native_stats: {} frames | draws {:.0f} (fallback {:.0f}) | drain avg {:.2f} ms "
+      "max {:.2f} ms | MakeRangeValid {:.0f} | watch rearms {:.0f} | RequestRange KiB "
+      "vtx {:.0f} tex {:.0f} | unique vtx KiB {:.0f} | index KiB {:.0f} | shared pages "
+      "{:.1f} | resolve dests {} (vtx overlaps {}) | "
+      "frames={} draws={} fallback_draws={} make_range_valid_calls={} watch_rearms={} "
+      "request_range_vertex_bytes={} request_range_texture_bytes={} "
+      "native_unique_vertex_bytes={} native_index_bytes={} shared_page_overlaps={} "
+      "cp_drain_us_sum={} cp_drain_us_max={} cp_drain_samples={} resolve_dests={} "
+      "resolve_vertex_overlaps={}",
+      s.frames, s.draws / frames, s.fallback_draws / frames,
+      s.drain_samples ? double(s.drain_us_sum) / double(s.drain_samples) / 1000.0 : 0.0,
+      double(s.drain_us_max) / 1000.0, s.make_range_valid_calls / frames,
+      s.watch_rearms / frames, s.request_range_vertex_bytes / frames / 1024.0,
+      s.request_range_texture_bytes / frames / 1024.0,
+      s.unique_vertex_bytes_sum / frames / 1024.0, s.index_bytes / frames / 1024.0,
+      s.shared_page_overlaps / frames, s.resolve_ranges.size(), s.resolve_vertex_overlaps,
+      s.frames, s.draws, s.fallback_draws, s.make_range_valid_calls, s.watch_rearms,
+      s.request_range_vertex_bytes, s.request_range_texture_bytes, s.unique_vertex_bytes_sum,
+      s.index_bytes, s.shared_page_overlaps, s.drain_us_sum, s.drain_us_max, s.drain_samples,
+      s.resolve_ranges.size(), s.resolve_vertex_overlaps);
+
+  s.frames = 0;
+  s.draws = 0;
+  s.fallback_draws = 0;
+  s.make_range_valid_calls = 0;
+  s.watch_rearms = 0;
+  s.request_range_vertex_bytes = 0;
+  s.request_range_texture_bytes = 0;
+  s.unique_vertex_bytes_sum = 0;
+  s.index_bytes = 0;
+  s.shared_page_overlaps = 0;
+  s.drain_us_sum = 0;
+  s.drain_us_max = 0;
+  s.drain_samples = 0;
+}
+
+}  // namespace rex::graphics::native_telemetry
diff --git a/src/graphics/pipeline/texture/cache.cpp b/src/graphics/pipeline/texture/cache.cpp
index c7c824e..4c1c2e3 100644
--- a/src/graphics/pipeline/texture/cache.cpp
+++ b/src/graphics/pipeline/texture/cache.cpp
@@ -25,6 +25,7 @@
 #include <rex/graphics/xenos.h>
 #include <rex/logging.h>
 #include <rex/math.h>
+#include <rex/perf/counter.h>
 
 REXCVAR_DEFINE_INT32(texture_cache_memory_limit_render_to_texture, 24, "GPU",
                      "Texture cache memory limit for render-to-texture (MB)")
@@ -566,8 +567,13 @@ void TextureCache::RequestTextures(uint32_t used_texture_mask) {
   std::vector<std::pair<uint32_t, uint32_t>> pending_shared_memory_range_pairs;
   if (!pending_shared_memory_ranges.empty()) {
     pending_shared_memory_range_pairs.reserve(pending_shared_memory_ranges.size());
+    bool collect_native_stats = REXCVAR_GET(gpu_native_stats);
     for (const PendingSharedMemoryRange& pending_range : pending_shared_memory_ranges) {
       pending_shared_memory_range_pairs.emplace_back(pending_range.start, pending_range.length);
+      PERF_counter_add(kRequestRangeTextureBytes, pending_range.length);
+      if (collect_native_stats) {
+        native_telemetry::NoteTextureRange(pending_range.start, pending_range.length);
+      }
     }
     batched_shared_memory_request_succeeded = shared_memory().RequestRanges(
         pending_shared_memory_range_pairs.data(), pending_shared_memory_range_pairs.size());
@@ -885,8 +891,13 @@ bool TextureCache::LoadTextureData(Texture& texture) {
   }
 
   std::pair<uint32_t, uint32_t> pending_range_pairs[2];
+  bool collect_native_stats = REXCVAR_GET(gpu_native_stats);
   for (size_t i = 0; i < pending_range_count; ++i) {
     pending_range_pairs[i] = std::make_pair(pending_ranges[i].start, pending_ranges[i].length);
+    PERF_counter_add(kRequestRangeTextureBytes, pending_ranges[i].length);
+    if (collect_native_stats) {
+      native_telemetry::NoteTextureRange(pending_ranges[i].start, pending_ranges[i].length);
+    }
   }
   if (!shared_memory().RequestRanges(pending_range_pairs, pending_range_count)) {
     return false;
diff --git a/src/graphics/primitive_processor.cpp b/src/graphics/primitive_processor.cpp
index 19ee54a..cb25391 100644
--- a/src/graphics/primitive_processor.cpp
+++ b/src/graphics/primitive_processor.cpp
@@ -26,6 +26,7 @@
 #include <rex/graphics/xenos.h>
 #include <rex/logging.h>
 #include <rex/math.h>
+#include <rex/perf/counter.h>
 #include <rex/types.h>
 
 REXCVAR_DEFINE_BOOL(force_convert_line_loops_to_strips, false, "GPU",
@@ -923,6 +924,10 @@ bool PrimitiveProcessor::Process(ProcessingResult& result_out) {
       cacheable.index_buffer_type == ProcessedIndexBufferType::kHostBuiltinForDMA) {
     // Request the index buffer memory.
     // TODO(Triang3l): Shared memory request cache.
+    PERF_counter_add(kNativeIndexBytes, guest_index_buffer_needed_bytes);
+    if (REXCVAR_GET(gpu_native_stats)) {
+      native_telemetry::NoteIndexRange(guest_index_base, guest_index_buffer_needed_bytes);
+    }
     if (!shared_memory_.RequestRange(guest_index_base, guest_index_buffer_needed_bytes)) {
       REXGPU_ERROR(
           "PrimitiveProcessor: Failed to request index buffer 0x{:08X}, 0x{:X} "
@@ -1373,6 +1378,7 @@ PrimitiveProcessor::CacheTransaction::CacheTransaction(PrimitiveProcessor& proce
           processor_.memory_.RegisterPhysicalMemoryInvalidationCallback(
               MemoryInvalidationCallbackThunk, &processor_);
     }
+    PERF_counter_inc(kWatchRearms);
     processor_.memory_.EnablePhysicalMemoryAccessCallbacks(key_.base, size_bytes, true, false);
   }
 }
diff --git a/src/graphics/shared_memory.cpp b/src/graphics/shared_memory.cpp
index 333becb..6a46876 100644
--- a/src/graphics/shared_memory.cpp
+++ b/src/graphics/shared_memory.cpp
@@ -19,6 +19,7 @@
 #include <rex/graphics/shared_memory.h>
 #include <rex/math.h>
 #include <rex/memory.h>
+#include <rex/perf/counter.h>
 
 namespace rex::graphics {
 
@@ -340,6 +341,10 @@ void SharedMemory::MakeRangeValid(uint32_t start, uint32_t length, bool written_
   if (length == 0 || start >= kBufferSize) {
     return;
   }
+  // Baseline for the native-residency work: this is the entry point that owns
+  // the write-watch arming (and with it the mprotect cost) on the emulated
+  // path.
+  PERF_counter_inc(kMakeRangeValidCalls);
   length = std::min(length, kBufferSize - start);
   uint32_t last = start + length - 1;
   uint32_t valid_page_first = start >> page_size_log2_;
@@ -374,6 +379,7 @@ void SharedMemory::MakeRangeValid(uint32_t start, uint32_t length, bool written_
   }
 
   if (memory_invalidation_callback_handle_) {
+    PERF_counter_inc(kWatchRearms);
     memory().EnablePhysicalMemoryAccessCallbacks(
         valid_page_first << page_size_log2_,
         (valid_page_last - valid_page_first + 1) << page_size_log2_, true, false);
diff --git a/src/graphics/vulkan/command_processor.cpp b/src/graphics/vulkan/command_processor.cpp
index a40436e..cdd9188 100644
--- a/src/graphics/vulkan/command_processor.cpp
+++ b/src/graphics/vulkan/command_processor.cpp
@@ -40,6 +40,7 @@
 #include <rex/graphics/vulkan/shared_memory.h>
 #include <rex/graphics/xenos.h>
 #include <rex/kernel/xboxkrnl/video.h>
+#include <rex/perf/counter.h>
 #include <rex/types.h>
 #include <rex/memory/utils.h>
 #include <rex/ui/flags.h>
@@ -73,6 +74,27 @@ namespace rex::graphics::vulkan {
 
 namespace {
 
+// True when draws should be dropped for the suppression oracle. Refuses to
+// engage alongside native residency (which it would silently invalidate) or
+// trace capture (whose golden traces must come from the emulated path); both
+// combinations are logged once and then ignored, leaving normal rendering.
+bool NativeSuppressDrawsActive() {
+  if (!REXCVAR_GET(gpu_native_suppress_draws)) {
+    return false;
+  }
+  if (REXCVAR_GET(gpu_native_residency) || REXCVAR_GET(trace_gpu_stream)) {
+    static bool refusal_logged = false;
+    if (!refusal_logged) {
+      refusal_logged = true;
+      REXGPU_WARN(
+          "gpu_native_suppress_draws ignored: it cannot be combined with "
+          "gpu_native_residency or trace_gpu_stream");
+    }
+    return false;
+  }
+  return true;
+}
+
 // glslang default built-in resource limits.
 constexpr TBuiltInResource kGlslangDefaultTBuiltInResource = {
     /* .maxLights = */ 32,
@@ -3629,6 +3651,16 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     return IssueCopy();
   }
 
+  // Suppression oracle. Diagnostic only, never a rendering mode: dropping the
+  // draw here skips the primitive processor, every shared memory request and
+  // the whole Vulkan issue path, which measures the frame rate ceiling of any
+  // approach that keeps the command processor in the loop. Resolves above are
+  // deliberately left running so the presenter still has a swap source and the
+  // run stays a comparable frame rate measurement; the image is garbage.
+  if (NativeSuppressDrawsActive()) {
+    return true;
+  }
+
   bool surface_pitch_is_zero = regs.Get<reg::RB_SURFACE_INFO>().surface_pitch == 0;
 
   const ui::vulkan::VulkanDevice::Properties& device_properties = GetVulkanDevice()->properties();
@@ -4022,11 +4054,17 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
                       vfetch_index, vfetch_constant.dword_0, vfetch_constant.dword_1);
           return false;
       }
+      // Recorded before the in-sync early-out below so the working set covers
+      // every range the frame references, not just the ones that missed it.
+      if (REXCVAR_GET(gpu_native_stats)) {
+        native_telemetry::NoteVertexRange(vfetch_constant.address << 2, vfetch_constant.size << 2);
+      }
       VertexBufferState& state = vertex_buffer_states_[vfetch_index];
       if (state.address == vfetch_constant.address && state.size == vfetch_constant.size) {
         vertex_buffers_in_sync_[vfetch_index >> 6] |= vfetch_bit;
         continue;
       }
+      PERF_counter_add(kRequestRangeVertexBytes, vfetch_constant.size << 2);
       if (!shared_memory_->RequestRange(vfetch_constant.address << 2, vfetch_constant.size << 2)) {
         REXGPU_ERROR(
             "Failed to request vertex buffer at 0x{:08X} (size {}) in the shared "
@@ -4406,8 +4444,12 @@ bool VulkanCommandProcessor::IssueCopy() {
   ReadbackResolveMode readback_mode = GetReadbackResolveMode(REXCVAR_GET(vulkan_readback_resolve));
   if (readback_mode == ReadbackResolveMode::kDisabled) {
     uint32_t written_address, written_length;
-    return render_target_cache_->Resolve(*memory_, *shared_memory_, *texture_cache_,
-                                         written_address, written_length);
+    bool resolved = render_target_cache_->Resolve(*memory_, *shared_memory_, *texture_cache_,
+                                                  written_address, written_length);
+    if (resolved && REXCVAR_GET(gpu_native_stats)) {
+      native_telemetry::NoteResolveDestination(written_address, written_length);
+    }
+    return resolved;
   }
 
   return IssueCopy_ReadbackResolvePath();
@@ -4423,6 +4465,9 @@ bool VulkanCommandProcessor::IssueCopy_ReadbackResolvePath() {
                                      written_length)) {
     return false;
   }
+  if (REXCVAR_GET(gpu_native_stats)) {
+    native_telemetry::NoteResolveDestination(written_address, written_length);
+  }
 
   if (!written_length) {
     return true;
diff --git a/src/graphics/vulkan/pipeline_cache.cpp b/src/graphics/vulkan/pipeline_cache.cpp
index 91ac08f..86edb1e 100644
--- a/src/graphics/vulkan/pipeline_cache.cpp
+++ b/src/graphics/vulkan/pipeline_cache.cpp
@@ -1246,6 +1246,16 @@ bool VulkanPipelineCache::TranslateAnalyzedShader(SpirvShaderTranslator& transla
     return false;
   }
 
+  // Dump shader files if desired. Mirrors the D3D12 pipeline cache so the
+  // Vulkan backend can produce the same per-shader artifacts.
+  if (!REXCVAR_GET(dump_shaders).empty()) {
+    bool edram_fsi_used =
+        render_target_cache_.GetPath() == RenderTargetCache::Path::kPixelShaderInterlock;
+    translation.Dump(REXCVAR_GET(dump_shaders), (shader.type() == xenos::ShaderType::kPixel)
+                                                    ? (edram_fsi_used ? "vulkan_fsi" : "vulkan_rtv")
+                                                    : "vulkan");
+  }
+
   // TODO(Triang3l): Log that the shader has been successfully translated in
   // common code.
 

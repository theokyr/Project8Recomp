diff --git a/include/rex/graphics/vulkan/command_processor.h b/include/rex/graphics/vulkan/command_processor.h
index 0eb9d4c..d422cd1 100644
--- a/include/rex/graphics/vulkan/command_processor.h
+++ b/include/rex/graphics/vulkan/command_processor.h
@@ -745,6 +745,19 @@ class VulkanCommandProcessor : public CommandProcessor {
   bool scratch_buffer_used_ = false;

   static constexpr uint32_t kMaxOcclusionQueries = 8192;
+  // GPU frame timing. Two timestamps per frame in flight, written into the
+  // guest's own command buffer at frame open and frame close, so what is
+  // measured is the GPU's view of the frame rather than the CPU's.
+  //
+  // Read back without VK_QUERY_RESULT_WAIT_BIT: a stall here would change the
+  // thing being measured, so a slot that is not ready yet is simply skipped.
+  static constexpr uint32_t kTimestampQueriesPerFrame = 2;
+  VkQueryPool timestamp_query_pool_ = VK_NULL_HANDLE;
+  bool timestamp_queries_available_ = false;
+  double timestamp_period_ns_ = 0.0;
+  // Frame number occupying each slot, or 0 when the slot holds nothing.
+  uint64_t timestamp_slot_frame_[kMaxFramesInFlight] = {};
+
   VkQueryPool occlusion_query_pool_ = VK_NULL_HANDLE;
   VkBuffer occlusion_query_readback_buffer_ = VK_NULL_HANDLE;
   VkDeviceMemory occlusion_query_readback_memory_ = VK_NULL_HANDLE;
diff --git a/include/rex/graphics/vulkan/deferred_command_buffer.h b/include/rex/graphics/vulkan/deferred_command_buffer.h
index 9d2a848..fe9ac62 100644
--- a/include/rex/graphics/vulkan/deferred_command_buffer.h
+++ b/include/rex/graphics/vulkan/deferred_command_buffer.h
@@ -273,6 +273,15 @@ class DeferredCommandBuffer {
     args.query_count = query_count;
   }

+  void CmdVkWriteTimestamp(VkPipelineStageFlagBits pipeline_stage, VkQueryPool query_pool,
+                           uint32_t query) {
+    auto& args = *reinterpret_cast<ArgsVkWriteTimestamp*>(
+        WriteCommand(Command::kVkWriteTimestamp, sizeof(ArgsVkWriteTimestamp)));
+    args.pipeline_stage = pipeline_stage;
+    args.query_pool = query_pool;
+    args.query = query;
+  }
+
   // pNext of all barriers must be null.
   void CmdVkPipelineBarrier(VkPipelineStageFlags src_stage_mask,
                             VkPipelineStageFlags dst_stage_mask, VkDependencyFlags dependency_flags,
@@ -374,6 +383,7 @@ class DeferredCommandBuffer {
     kVkPipelineBarrier,
     kVkPushConstants,
     kVkResetQueryPool,
+    kVkWriteTimestamp,
     kVkSetBlendConstants,
     kVkSetDepthBias,
     kVkSetScissor,
@@ -520,6 +530,12 @@ class DeferredCommandBuffer {
     uint32_t query;
   };

+  struct ArgsVkWriteTimestamp {
+    VkPipelineStageFlagBits pipeline_stage;
+    VkQueryPool query_pool;
+    uint32_t query;
+  };
+
   struct ArgsVkResetQueryPool {
     VkQueryPool query_pool;
     uint32_t first_query;
diff --git a/include/rex/perf/counter.h b/include/rex/perf/counter.h
index 382b455..4125a01 100644
--- a/include/rex/perf/counter.h
+++ b/include/rex/perf/counter.h
@@ -93,6 +93,60 @@ enum class CounterId : uint16_t {
   // hard zero across a long soak says it never fires in this title.
   kNativeSeqEvictions,

+  // GPU time for one frame, in microseconds, measured on the GPU itself with
+  // a pair of Vulkan timestamp queries rather than inferred from wall clock.
+  //
+  // Exists because every other instrument here is CPU-side, and on an APU that
+  // is only half the picture: a change that uploads fewer bytes gives the GPU
+  // less to do as well as the CPU, and nothing in this table could tell those
+  // apart. `gpu_busy_percent` is not a substitute - it reads 0 on Van Gogh even
+  // mid-render.
+  //
+  // READ THIS BEFORE QUOTING IT. This is the SPAN from the frame's first GPU
+  // command starting to its last completing, not GPU-busy time: if the GPU goes
+  // idle mid-frame waiting for the CPU to submit more, that idle time is inside
+  // the number. Measured on a Deck it reads 82-86% of frame time in arms whose
+  // CPU cost differs by 3x, which is the giveaway.
+  //
+  // It also perturbs: enabling it cost ~8 ms/frame on one arm and ~14 ms on
+  // another, enough to erase the difference between them. Use it to see whether
+  // GPU work exists at all, never to compare two configurations.
+  //
+  // Zero when gpu_timestamp_queries is off (the default) or when the device
+  // reports no timestampPeriod.
+  kGpuFrameUs,
+
+  // Draws whose shader writes a memory-export register
+  // (`memexport_eM_written() != 0`). Two things follow from a draw being one,
+  // and both are expensive: the native residency path is disarmed for it
+  // (BeginDraw(!memexport_writes_possible)), and shared memory is Use()d for
+  // read-WRITE rather than read, which is a barrier the GPU must honour.
+  //
+  // On THPS P8 this is ~11% of draws, and it is exactly the gap between
+  // kDrawCalls and kNativeDraws - which is how the count was inferred before
+  // this counter existed. It cannot be inferred once a draw can be excluded
+  // from the native path for any other reason, hence the counter.
+  kMemexportDraws,
+
+  // The subset of those whose destination ranges did not resolve, so the
+  // conservative path is taken: RequestRange over the WHOLE shared memory
+  // buffer, and a read-write barrier over the whole buffer, per draw. If this
+  // is nonzero in steady state it is the first thing to fix; if it is zero the
+  // barriers are at least scoped to real extents.
+  kMemexportUnresolved,
+
+  // Vulkan render pass entries per frame. On this backend a barrier ENDS the
+  // render pass (SubmitBarriers calls EndRenderPass whenever anything is
+  // pending) and the next draw re-enters it. If this approaches the draw count
+  // the GPU is tearing down and restoring the framebuffer per draw, which costs
+  // thousands of clocks and produces no pixels - the signature being GPU time
+  // that scales with clock but not with resolution.
+  kRenderPassBegins,
+
+  // vkCmdPipelineBarrier commands actually emitted per frame. Pairs with the
+  // above: this is what forces those render pass ends.
+  kVkPipelineBarriers,
+
   kCount  // sentinel -- must be last
 };

diff --git a/include/rex/ui/vulkan/device.h b/include/rex/ui/vulkan/device.h
index 947817f..d371b91 100644
--- a/include/rex/ui/vulkan/device.h
+++ b/include/rex/ui/vulkan/device.h
@@ -71,6 +71,9 @@ class VulkanDevice {
     uint32_t maxFragmentInputComponents = 64;
     uint32_t maxFragmentCombinedOutputResources = 4;
     float maxSamplerAnisotropy = 1.0f;
+    // Nanoseconds per timestamp tick. 0 means the device cannot timestamp, so
+    // it doubles as the "are GPU timings available" flag.
+    float timestampPeriod = 0.0f;
     uint32_t maxViewportDimensions[2] = {4096, 4096};
     VkDeviceSize minUniformBufferOffsetAlignment = 256;
     VkDeviceSize minStorageBufferOffsetAlignment = 256;
diff --git a/include/rex/ui/vulkan/functions/device_1_0.inc b/include/rex/ui/vulkan/functions/device_1_0.inc
index aa55a4a..c4461f4 100644
--- a/include/rex/ui/vulkan/functions/device_1_0.inc
+++ b/include/rex/ui/vulkan/functions/device_1_0.inc
@@ -25,6 +25,7 @@ XE_UI_VULKAN_FUNCTION(vkCmdEndQuery)
 XE_UI_VULKAN_FUNCTION(vkCmdPipelineBarrier)
 XE_UI_VULKAN_FUNCTION(vkCmdPushConstants)
 XE_UI_VULKAN_FUNCTION(vkCmdResetQueryPool)
+XE_UI_VULKAN_FUNCTION(vkCmdWriteTimestamp)
 XE_UI_VULKAN_FUNCTION(vkCmdSetBlendConstants)
 XE_UI_VULKAN_FUNCTION(vkCmdSetDepthBias)
 XE_UI_VULKAN_FUNCTION(vkCmdSetScissor)
@@ -61,6 +62,7 @@ XE_UI_VULKAN_FUNCTION(vkDestroyImageView)
 XE_UI_VULKAN_FUNCTION(vkDestroyPipeline)
 XE_UI_VULKAN_FUNCTION(vkDestroyPipelineLayout)
 XE_UI_VULKAN_FUNCTION(vkDestroyQueryPool)
+XE_UI_VULKAN_FUNCTION(vkGetQueryPoolResults)
 XE_UI_VULKAN_FUNCTION(vkDestroyRenderPass)
 XE_UI_VULKAN_FUNCTION(vkDestroySampler)
 XE_UI_VULKAN_FUNCTION(vkDestroySemaphore)
diff --git a/src/core/perf/counter.cpp b/src/core/perf/counter.cpp
index 3d64806..26f1cdd 100644
--- a/src/core/perf/counter.cpp
+++ b/src/core/perf/counter.cpp
@@ -63,6 +63,11 @@ constexpr const char* kCounterNames[] = {
     "cp_drain_us",
     "stale_protect_recoveries",
     "native_seq_evictions",
+    "gpu_frame_us",
+    "memexport_draws",
+    "memexport_unresolved",
+    "render_pass_begins",
+    "vk_pipeline_barriers",
 };
 static_assert(std::size(kCounterNames) == kNumCounters, "kCounterNames must match CounterId enum");

@@ -100,6 +105,11 @@ constexpr bool kIsGauge[] = {
     false,  // kCpDrainUs                 (set once per frame)
     false,  // kStaleProtectRecoveries
     false,  // kNativeSeqEvictions
+    false,  // kGpuFrameUs                (set once per frame)
+    false,  // kMemexportDraws
+    false,  // kMemexportUnresolved
+    false,  // kRenderPassBegins
+    false,  // kVkPipelineBarriers
 };
 static_assert(std::size(kIsGauge) == kNumCounters, "kIsGauge must match CounterId enum");

diff --git a/src/graphics/vulkan/command_processor.cpp b/src/graphics/vulkan/command_processor.cpp
index 181b08a..41eae62 100644
--- a/src/graphics/vulkan/command_processor.cpp
+++ b/src/graphics/vulkan/command_processor.cpp
@@ -53,6 +53,11 @@
 #include "native_residency.h"

 // Legacy backend compatibility aliases for shared readback controls.
+REXCVAR_DEFINE_BOOL(gpu_timestamp_queries, false, "GPU",
+                    "Measure GPU time per frame with a pair of Vulkan timestamp queries and "
+                    "publish it as the gpu_frame_us counter. Every other counter here is "
+                    "CPU-side, which on an APU is only half the picture.")
+    .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
 REXCVAR_DEFINE_BOOL(vulkan_readback_resolve, false, "GPU/Vulkan",
                     "Read render-to-texture results on the CPU")
     .lifecycle(rex::cvar::Lifecycle::kHotReload);
@@ -3133,6 +3138,7 @@ bool VulkanCommandProcessor::SubmitBarriers(bool force_end_render_pass) {
     // .data() + offset, not &[offset], for buffer and image barriers, because
     // if there are no buffer or image memory barriers in the last pipeline
     // barriers, the offsets may be equal to the sizes of the vectors.
+    rex::perf::IncrementCounter(rex::perf::CounterId::kVkPipelineBarriers);
     deferred_command_buffer_.CmdVkPipelineBarrier(
         it->src_stage_mask ? it->src_stage_mask : VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
         it->dst_stage_mask ? it->dst_stage_mask : VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT, 0, 0,
@@ -3224,6 +3230,7 @@ void VulkanCommandProcessor::SubmitBarriersAndEnterRenderTargetCacheRenderPass(
     deferred_command_buffer_.CmdVkBeginRenderPass(&render_pass_begin_info,
                                                   VK_SUBPASS_CONTENTS_INLINE);
   }
+  rex::perf::IncrementCounter(rex::perf::CounterId::kRenderPassBegins);
   in_render_pass_ = true;
 }

@@ -3316,6 +3323,7 @@ void VulkanCommandProcessor::SubmitBarriersAndEnterRenderTargetCacheRenderPass(
     deferred_command_buffer_.CmdVkBeginRenderPass(&render_pass_begin_info,
                                                   VK_SUBPASS_CONTENTS_INLINE);
   }
+  rex::perf::IncrementCounter(rex::perf::CounterId::kRenderPassBegins);
   in_render_pass_ = true;
 }

@@ -3776,6 +3784,9 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
   // draws are excluded unconditionally: their shaders read the index buffer
   // out of the shared memory buffer themselves, and the emulated path also
   // snapshots guest DMA indices for them to avoid aliasing through it.
+  if (memexport_writes_possible) {
+    rex::perf::IncrementCounter(rex::perf::CounterId::kMemexportDraws);
+  }
   native_residency_->BeginDraw(!memexport_writes_possible);

   // Two iterations because a submission (even the current one - in which case
@@ -4161,6 +4172,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
         std::max(memexport_extent_end, memexport_range_base_bytes + memexport_range.size_bytes);
   }
   if (memexport_writes_possible && memexport_ranges_.empty()) {
+    rex::perf::IncrementCounter(rex::perf::CounterId::kMemexportUnresolved);
     if (!shared_memory_->RequestRange(0, SharedMemory::kBufferSize)) {
       REXGPU_ERROR(
           "Failed to request full shared memory residency for unresolved "
@@ -4884,6 +4896,29 @@ bool VulkanCommandProcessor::InitializeOcclusionQueryResources() {
   const ui::vulkan::VulkanDevice::Functions& dfn = vulkan_device->functions();
   VkDevice device = vulkan_device->device();

+  // GPU frame timing, if asked for. Independent of the occlusion pool below:
+  // its failure must not take occlusion queries down with it, and vice versa.
+  if (REXCVAR_GET(gpu_timestamp_queries)) {
+    const float timestamp_period = vulkan_device->properties().timestampPeriod;
+    if (timestamp_period <= 0.0f) {
+      REXGPU_WARN("gpu_timestamp_queries: device reports timestampPeriod 0; disabled");
+    } else {
+      VkQueryPoolCreateInfo ts_info = {};
+      ts_info.sType = VK_STRUCTURE_TYPE_QUERY_POOL_CREATE_INFO;
+      ts_info.queryType = VK_QUERY_TYPE_TIMESTAMP;
+      ts_info.queryCount = kTimestampQueriesPerFrame * kMaxFramesInFlight;
+      if (dfn.vkCreateQueryPool(device, &ts_info, nullptr, &timestamp_query_pool_) != VK_SUCCESS) {
+        REXGPU_WARN("gpu_timestamp_queries: failed to create the query pool; disabled");
+        timestamp_query_pool_ = VK_NULL_HANDLE;
+      } else {
+        timestamp_period_ns_ = double(timestamp_period);
+        timestamp_queries_available_ = true;
+        REXGPU_INFO("gpu_timestamp_queries: enabled, timestampPeriod {} ns",
+                    timestamp_period_ns_);
+      }
+    }
+  }
+
   VkQueryPoolCreateInfo pool_info = {};
   pool_info.sType = VK_STRUCTURE_TYPE_QUERY_POOL_CREATE_INFO;
   pool_info.queryType = VK_QUERY_TYPE_OCCLUSION;
@@ -4939,6 +4974,12 @@ void VulkanCommandProcessor::ShutdownOcclusionQueryResources() {
   if (occlusion_query_pool_ != VK_NULL_HANDLE) {
     dfn.vkDestroyQueryPool(device, occlusion_query_pool_, nullptr);
   }
+  if (timestamp_query_pool_ != VK_NULL_HANDLE) {
+    dfn.vkDestroyQueryPool(device, timestamp_query_pool_, nullptr);
+    timestamp_query_pool_ = VK_NULL_HANDLE;
+  }
+  timestamp_queries_available_ = false;
+  std::memset(timestamp_slot_frame_, 0, sizeof(timestamp_slot_frame_));

   occlusion_query_pool_ = VK_NULL_HANDLE;
   occlusion_query_readback_buffer_ = VK_NULL_HANDLE;
@@ -5330,6 +5371,45 @@ bool VulkanCommandProcessor::BeginSubmission(bool is_guest_command) {
     frame_open_ = true;
     frame_used_async_placeholder_pipeline_ = false;

+    // Harvest any finished frame's timestamps first. frame_completed_ was just
+    // brought up to date above, so a slot whose frame is at or below it has
+    // certainly executed; the read is still issued without WAIT so that a
+    // driver disagreeing about that cannot stall the command processor.
+    if (timestamp_queries_available_) {
+      const ui::vulkan::VulkanDevice* const ts_device = GetVulkanDevice();
+      const ui::vulkan::VulkanDevice::Functions& ts_dfn = ts_device->functions();
+      for (uint32_t slot = 0; slot < kMaxFramesInFlight; ++slot) {
+        const uint64_t slot_frame = timestamp_slot_frame_[slot];
+        if (!slot_frame || slot_frame > frame_completed_) {
+          continue;
+        }
+        uint64_t stamps[kTimestampQueriesPerFrame] = {};
+        if (ts_dfn.vkGetQueryPoolResults(
+                ts_device->device(), timestamp_query_pool_,
+                slot * kTimestampQueriesPerFrame, kTimestampQueriesPerFrame, sizeof(stamps),
+                stamps, sizeof(uint64_t), VK_QUERY_RESULT_64_BIT) == VK_SUCCESS) {
+          if (stamps[1] > stamps[0]) {
+            const double ns = double(stamps[1] - stamps[0]) * timestamp_period_ns_;
+            rex::perf::SetCounter(rex::perf::CounterId::kGpuFrameUs, int64_t(ns / 1000.0));
+          }
+          timestamp_slot_frame_[slot] = 0;
+        }
+      }
+    }
+
+    if (timestamp_queries_available_) {
+      const uint32_t slot = uint32_t(frame_current_ % kMaxFramesInFlight);
+      const uint32_t first = slot * kTimestampQueriesPerFrame;
+      // Reset both of the slot's queries inside the command buffer, so the
+      // reset is ordered against the GPU's own execution rather than against
+      // the host, and cannot race a still-in-flight frame reading them.
+      deferred_command_buffer_.CmdVkResetQueryPool(timestamp_query_pool_, first,
+                                                   kTimestampQueriesPerFrame);
+      deferred_command_buffer_.CmdVkWriteTimestamp(VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
+                                                   timestamp_query_pool_, first);
+      timestamp_slot_frame_[slot] = frame_current_;
+    }
+
     // Reset bindings that depend on transient data.
     std::memset(current_float_constant_map_vertex_, 0, sizeof(current_float_constant_map_vertex_));
     std::memset(current_float_constant_map_pixel_, 0, sizeof(current_float_constant_map_pixel_));
@@ -5457,6 +5537,15 @@ bool VulkanCommandProcessor::EndSubmission(bool is_swap) {

   bool is_closing_frame = is_swap && frame_open_;

+  // Before the submit block below, which is the last point at which anything
+  // can still be recorded into this frame's command buffer.
+  if (is_closing_frame && timestamp_queries_available_ && submission_open_) {
+    const uint32_t slot = uint32_t(frame_current_ % kMaxFramesInFlight);
+    deferred_command_buffer_.CmdVkWriteTimestamp(
+        VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT, timestamp_query_pool_,
+        slot * kTimestampQueriesPerFrame + 1);
+  }
+
   if (is_closing_frame) {
     texture_cache_->EndFrame();

diff --git a/src/graphics/vulkan/deferred_command_buffer.cpp b/src/graphics/vulkan/deferred_command_buffer.cpp
index b49e033..ad60ade 100644
--- a/src/graphics/vulkan/deferred_command_buffer.cpp
+++ b/src/graphics/vulkan/deferred_command_buffer.cpp
@@ -272,6 +272,11 @@ void DeferredCommandBuffer::Execute(VkCommandBuffer command_buffer) {
                                 args.query_count);
       } break;

+      case Command::kVkWriteTimestamp: {
+        auto& args = *reinterpret_cast<const ArgsVkWriteTimestamp*>(stream);
+        dfn.vkCmdWriteTimestamp(command_buffer, args.pipeline_stage, args.query_pool, args.query);
+      } break;
+
       case Command::kVkSetBlendConstants: {
         auto& args = *reinterpret_cast<const ArgsVkSetBlendConstants*>(stream);
         dfn.vkCmdSetBlendConstants(command_buffer, args.blend_constants);
diff --git a/src/ui/vulkan/vulkan_device.cpp b/src/ui/vulkan/vulkan_device.cpp
index fc31478..70fe659 100644
--- a/src/ui/vulkan/vulkan_device.cpp
+++ b/src/ui/vulkan/vulkan_device.cpp
@@ -588,6 +588,7 @@ std::unique_ptr<VulkanDevice> VulkanDevice::CreateIfSupported(
                 properties_1_2_KHR_driver_properties.conformanceVersion.patch);
   }

+  XE_UI_VULKAN_LIMIT(timestampPeriod)
   XE_UI_VULKAN_LIMIT(maxImageDimension2D)
   XE_UI_VULKAN_LIMIT(maxImageDimension3D)
   XE_UI_VULKAN_LIMIT(maxImageDimensionCube)

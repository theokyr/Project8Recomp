diff --git a/include/rex/graphics/flags.h b/include/rex/graphics/flags.h
index 034a451..cc6739d 100644
--- a/include/rex/graphics/flags.h
+++ b/include/rex/graphics/flags.h
@@ -85,6 +85,7 @@ REXCVAR_DECLARE(uint32_t, gpu_native_hash_max_bytes);
 REXCVAR_DECLARE(uint32_t, gpu_native_hash_min_bytes);
 REXCVAR_DECLARE(bool, gpu_native_stats);
 REXCVAR_DECLARE(bool, gpu_native_suppress_draws);
+REXCVAR_DECLARE(bool, gpu_cp_fastpath);
 
 // GPU Shader Translation / Tracing
 REXCVAR_DECLARE(std::string, dump_shaders);
@@ -106,6 +107,7 @@ REXCVAR_DECLARE(std::string, render_target_path_vulkan);
 // Legacy backend compatibility aliases for shared readback controls.
 REXCVAR_DECLARE(bool, vulkan_readback_resolve);
 REXCVAR_DECLARE(bool, vulkan_readback_memexport);
+REXCVAR_DECLARE(bool, gpu_texture_request_reuse);
 #endif  // REX_HAS_VULKAN
 
 #if REX_HAS_D3D12
diff --git a/include/rex/graphics/pipeline/texture/cache.h b/include/rex/graphics/pipeline/texture/cache.h
index 24a25bd..25fed53 100644
--- a/include/rex/graphics/pipeline/texture/cache.h
+++ b/include/rex/graphics/pipeline/texture/cache.h
@@ -130,6 +130,11 @@ class TextureCache {
            (binding->texture_signed && binding->texture_signed->key().scaled_resolve);
   }
 
+  // Copies the normalized 16-byte-or-smaller cache key for an active binding.
+  // This exposes layout identity without exposing texture contents or the
+  // protected TextureKey type itself.
+  bool GetActiveTextureCensusKey(uint32_t fetch_constant_index, uint8_t key_out[16]) const;
+
  protected:
   struct TextureKey {
     // Dimensions minus 1 are stored similarly to how they're stored in fetch
diff --git a/include/rex/graphics/vulkan/command_processor.h b/include/rex/graphics/vulkan/command_processor.h
index 33acbb8..52fabe1 100644
--- a/include/rex/graphics/vulkan/command_processor.h
+++ b/include/rex/graphics/vulkan/command_processor.h
@@ -47,6 +47,9 @@ namespace rex::graphics::vulkan {
 
 // Plugin-internal, defined in src/graphics/vulkan/native_residency.h.
 class NativeResidency;
+namespace draw_census {
+class Recorder;
+}
 
 class VulkanCommandProcessor : public CommandProcessor {
  public:
@@ -498,6 +501,8 @@ class VulkanCommandProcessor : public CommandProcessor {
   };
   bool EnsureSwapFxaaSourceImage(uint32_t width, uint32_t height);
   void DestroySwapFxaaSourceImage();
+  void RecordDrawCensusCopy(uint32_t written_address, uint32_t written_length,
+                            bool readback_path);
 
   void UpdateDynamicState(const draw_util::ViewportInfo& viewport_info, bool primitive_polygonal,
                           reg::RB_DEPTHCONTROL normalized_depth_control);
@@ -626,6 +631,14 @@ class VulkanCommandProcessor : public CommandProcessor {
 
   std::unique_ptr<NativeResidency> native_residency_;
 
+  // Allocated lazily on the GPU thread only after gpu_census_capture is
+  // requested. The normal path keeps only a null check at each hook site.
+  std::unique_ptr<draw_census::Recorder> draw_census_;
+  uint64_t draw_census_swap_ordinal_ = 0;
+  uint64_t draw_census_float_dirty_[8] = {};
+  uint32_t draw_census_fetch_dirty_[6] = {};
+  uint32_t draw_census_boolloop_dirty_writes_ = 0;
+
   std::unique_ptr<VulkanRenderTargetCache> render_target_cache_;
 
   std::unique_ptr<VulkanPipelineCache> pipeline_cache_;
diff --git a/include/rex/graphics/vulkan/pipeline_cache.h b/include/rex/graphics/vulkan/pipeline_cache.h
index 7b938b9..e064a95 100644
--- a/include/rex/graphics/vulkan/pipeline_cache.h
+++ b/include/rex/graphics/vulkan/pipeline_cache.h
@@ -93,6 +93,11 @@ class VulkanPipelineCache {
       reg::RB_DEPTHCONTROL normalized_depth_control, uint32_t normalized_color_mask,
       VulkanRenderTargetCache::RenderPassKey render_pass_key, VkPipeline& pipeline_out,
       const PipelineLayoutProvider*& pipeline_layout_out, void** pipeline_handle_out = nullptr);
+  // Valid after ConfigurePipeline succeeds. Census-only readout of the exact
+  // packed description already used as the cache key.
+  uint64_t last_pipeline_description_hash() const {
+    return last_pipeline_ ? last_pipeline_->first.GetHash() : 0;
+  }
   bool IsCreatingPipelines() const;
   void GetPipelineAndLayoutByHandle(void* handle, VkPipeline& pipeline_out,
                                     const PipelineLayoutProvider*& pipeline_layout_out,
diff --git a/src/graphics/CMakeLists.txt b/src/graphics/CMakeLists.txt
index 72b0cba..4109e89 100644
--- a/src/graphics/CMakeLists.txt
+++ b/src/graphics/CMakeLists.txt
@@ -47,6 +47,7 @@ set(REXGPU_XENOS_VULKAN_SOURCES
     vulkan/graphics_system.cpp
     vulkan/deferred_command_buffer.cpp
     vulkan/command_processor.cpp
+    vulkan/draw_census.cc
     vulkan/native_residency.cc
     vulkan/pipeline_cache.cpp
     vulkan/primitive_processor.cpp
diff --git a/src/graphics/pipeline/texture/cache.cpp b/src/graphics/pipeline/texture/cache.cpp
index 4c1c2e3..a80b977 100644
--- a/src/graphics/pipeline/texture/cache.cpp
+++ b/src/graphics/pipeline/texture/cache.cpp
@@ -230,6 +230,18 @@ TextureCache::~TextureCache() {
   }
 }
 
+bool TextureCache::GetActiveTextureCensusKey(uint32_t fetch_constant_index,
+                                             uint8_t key_out[16]) const {
+  static_assert(sizeof(TextureKey) <= 16);
+  std::memset(key_out, 0, 16);
+  const TextureBinding* binding = GetValidTextureBinding(fetch_constant_index);
+  if (!binding) {
+    return false;
+  }
+  std::memcpy(key_out, &binding->key, sizeof(TextureKey));
+  return true;
+}
+
 bool TextureCache::GetConfigDrawResolutionScale(uint32_t& x_out, uint32_t& y_out) {
   uint32_t shared_scale = uint32_t(std::max(INT32_C(1), REXCVAR_GET(resolution_scale)));
   bool use_shared_scale = rex::cvar::HasNonDefaultValue("resolution_scale");
diff --git a/src/graphics/vulkan/command_processor.cpp b/src/graphics/vulkan/command_processor.cpp
index e194b13..f50344b 100644
--- a/src/graphics/vulkan/command_processor.cpp
+++ b/src/graphics/vulkan/command_processor.cpp
@@ -12,6 +12,7 @@
 #include <algorithm>
 #include <array>
 #include <atomic>
+#include <bit>
 #include <chrono>
 #include <cstdint>
 #include <cstring>
@@ -28,6 +29,7 @@
 #include <rex/assert.h>
 #include <rex/cvar.h>
 #include <rex/dbg.h>
+#include <rex/hash.h>
 #include <rex/logging.h>
 #include <rex/math.h>
 #include <rex/graphics/util/draw.h>
@@ -43,6 +45,7 @@
 #include <rex/graphics/xenos.h>
 #include <rex/kernel/xboxkrnl/video.h>
 #include <rex/perf/counter.h>
+#include <rex/system/kernel_state.h>
 #include <rex/types.h>
 #include <rex/memory/utils.h>
 #include <rex/ui/flags.h>
@@ -50,6 +53,7 @@
 #include <rex/ui/vulkan/util.h>
 
 // Plugin-internal, so not under include/rex.
+#include "draw_census.h"
 #include "native_residency.h"
 
 // Legacy backend compatibility aliases for shared readback controls.
@@ -108,6 +112,69 @@ bool NativeSuppressDrawsActive() {
   return true;
 }
 
+class CensusDrawAttempt {
+ public:
+  explicit CensusDrawAttempt(draw_census::Recorder* recorder) : recorder_(recorder) {}
+  ~CensusDrawAttempt() {
+    if (recorder_ && !committed_) {
+      recorder_->NoteDrawSkipped();
+    }
+  }
+  void Commit() { committed_ = true; }
+
+ private:
+  draw_census::Recorder* recorder_;
+  bool committed_ = false;
+};
+
+draw_census::ShaderRecord MakeCensusShaderRecord(const VulkanShader& shader) {
+  draw_census::ShaderRecord record{};
+  const Shader::ConstantRegisterMap& constants = shader.constant_register_map();
+  record.ucode_hash = shader.ucode_data_hash();
+  record.shader_type = uint8_t(shader.type());
+  record.memexport = shader.memexport_eM_written() != 0;
+  record.vertex_binding_count =
+      uint16_t(std::min<size_t>(shader.vertex_bindings().size(), UINT16_MAX));
+  record.ucode_dword_count = uint32_t(shader.ucode_dword_count());
+  record.float_count = constants.float_count;
+  std::memcpy(record.float_bitmap, constants.float_bitmap, sizeof(record.float_bitmap));
+  std::memcpy(record.bool_bitmap, constants.bool_bitmap, sizeof(record.bool_bitmap));
+  record.loop_bitmap = constants.loop_bitmap;
+  std::memcpy(record.vertex_fetch_bitmap, constants.vertex_fetch_bitmap,
+              sizeof(record.vertex_fetch_bitmap));
+  record.texture_mask = shader.GetUsedTextureMaskAfterTranslation();
+
+  // Hash explicit-width fields one by one so no host padding or enum width can
+  // enter the canonical declared-layout identity.
+  XXH3_state_t hash_state;
+  XXH3_64bits_reset(&hash_state);
+  for (const Shader::VertexBinding& binding : shader.vertex_bindings()) {
+    const uint32_t fetch_constant = binding.fetch_constant;
+    const uint32_t stride_words = binding.stride_words;
+    const uint32_t attribute_count = uint32_t(binding.attributes.size());
+    XXH3_64bits_update(&hash_state, &fetch_constant, sizeof(fetch_constant));
+    XXH3_64bits_update(&hash_state, &stride_words, sizeof(stride_words));
+    XXH3_64bits_update(&hash_state, &attribute_count, sizeof(attribute_count));
+    for (const Shader::VertexBinding::Attribute& attribute : binding.attributes) {
+      const ParsedVertexFetchInstruction::Attributes& layout = attribute.fetch_instr.attributes;
+      const uint32_t data_format = uint32_t(layout.data_format);
+      const uint32_t signed_rf_mode = uint32_t(layout.signed_rf_mode);
+      const uint8_t flags = uint8_t(layout.is_index_rounded) |
+                            (uint8_t(layout.is_signed) << 1) |
+                            (uint8_t(layout.is_integer) << 2);
+      XXH3_64bits_update(&hash_state, &data_format, sizeof(data_format));
+      XXH3_64bits_update(&hash_state, &layout.offset, sizeof(layout.offset));
+      XXH3_64bits_update(&hash_state, &layout.stride, sizeof(layout.stride));
+      XXH3_64bits_update(&hash_state, &layout.exp_adjust, sizeof(layout.exp_adjust));
+      XXH3_64bits_update(&hash_state, &layout.prefetch_count, sizeof(layout.prefetch_count));
+      XXH3_64bits_update(&hash_state, &signed_rf_mode, sizeof(signed_rf_mode));
+      XXH3_64bits_update(&hash_state, &flags, sizeof(flags));
+    }
+  }
+  record.vertex_layout_hash = XXH3_64bits_digest(&hash_state);
+  return record;
+}
+
 // glslang default built-in resource limits.
 constexpr TBuiltInResource kGlslangDefaultTBuiltInResource = {
     /* .maxLights = */ 32,
@@ -2175,6 +2242,21 @@ void VulkanCommandProcessor::ShutdownContext() {
 void VulkanCommandProcessor::WriteRegister(uint32_t index, uint32_t value) {
   CommandProcessor::WriteRegister(index, value);
 
+  if (draw_census_ && draw_census_->IsRecording()) {
+    if (index >= XE_GPU_REG_SHADER_CONSTANT_000_X &&
+        index <= XE_GPU_REG_SHADER_CONSTANT_511_W) {
+      const uint32_t vector = (index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
+      draw_census_float_dirty_[vector >> 6] |= UINT64_C(1) << (vector & 63);
+    } else if (index >= XE_GPU_REG_SHADER_CONSTANT_BOOL_000_031 &&
+               index <= XE_GPU_REG_SHADER_CONSTANT_LOOP_31) {
+      ++draw_census_boolloop_dirty_writes_;
+    } else if (index >= XE_GPU_REG_SHADER_CONSTANT_FETCH_00_0 &&
+               index <= XE_GPU_REG_SHADER_CONSTANT_FETCH_31_5) {
+      const uint32_t dword = index - XE_GPU_REG_SHADER_CONSTANT_FETCH_00_0;
+      draw_census_fetch_dirty_[dword >> 5] |= UINT32_C(1) << (dword & 31);
+    }
+  }
+
   if (index >= XE_GPU_REG_SHADER_CONSTANT_000_X && index <= XE_GPU_REG_SHADER_CONSTANT_511_W) {
     if (frame_open_) {
       uint32_t float_constant_index = (index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
@@ -2245,6 +2327,13 @@ void VulkanCommandProcessor::WriteRegistersFromMem(uint32_t start_index, uint32_
   if (start_index >= XE_GPU_REG_SHADER_CONSTANT_000_X &&
       end_index <= XE_GPU_REG_SHADER_CONSTANT_511_W) {
     memory::copy_and_swap(register_file_->values + start_index, base, num_registers);
+    if (draw_census_ && draw_census_->IsRecording()) {
+      const uint32_t first_vector = (start_index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
+      const uint32_t last_vector = (end_index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
+      for (uint32_t vector = first_vector; vector <= last_vector; ++vector) {
+        draw_census_float_dirty_[vector >> 6] |= UINT64_C(1) << (vector & 63);
+      }
+    }
     if (frame_open_) {
       uint32_t first_float_constant = (start_index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
       uint32_t last_float_constant = (end_index - XE_GPU_REG_SHADER_CONSTANT_000_X) >> 2;
@@ -2273,6 +2362,9 @@ void VulkanCommandProcessor::WriteRegistersFromMem(uint32_t start_index, uint32_
   if (start_index >= XE_GPU_REG_SHADER_CONSTANT_BOOL_000_031 &&
       end_index <= XE_GPU_REG_SHADER_CONSTANT_LOOP_31) {
     memory::copy_and_swap(register_file_->values + start_index, base, num_registers);
+    if (draw_census_ && draw_census_->IsRecording()) {
+      draw_census_boolloop_dirty_writes_ += num_registers;
+    }
     current_constant_buffers_up_to_date_ &=
         ~(UINT32_C(1) << SpirvShaderTranslator::kConstantBufferBoolLoop);
     return;
@@ -2285,6 +2377,11 @@ void VulkanCommandProcessor::WriteRegistersFromMem(uint32_t start_index, uint32_
         ~(UINT32_C(1) << SpirvShaderTranslator::kConstantBufferFetch);
     uint32_t first_fetch_dword = start_index - XE_GPU_REG_SHADER_CONSTANT_FETCH_00_0;
     uint32_t last_fetch_dword = end_index - XE_GPU_REG_SHADER_CONSTANT_FETCH_00_0;
+    if (draw_census_ && draw_census_->IsRecording()) {
+      for (uint32_t dword = first_fetch_dword; dword <= last_fetch_dword; ++dword) {
+        draw_census_fetch_dirty_[dword >> 5] |= UINT32_C(1) << (dword & 31);
+      }
+    }
     if (texture_cache_) {
       texture_cache_->TextureFetchConstantsWritten(first_fetch_dword / 6, last_fetch_dword / 6);
     }
@@ -2320,6 +2417,7 @@ void VulkanCommandProcessor::OnGammaRampPWLValueWritten() {
 
 void VulkanCommandProcessor::IssueSwap(uint32_t frontbuffer_ptr, uint32_t frontbuffer_width,
                                        uint32_t frontbuffer_height) {
+  ++draw_census_swap_ordinal_;
   SCOPE_profile_cpu_f("gpu");
   vertex_buffers_in_sync_[0] = 0;
   vertex_buffers_in_sync_[1] = 0;
@@ -2331,6 +2429,45 @@ void VulkanCommandProcessor::IssueSwap(uint32_t frontbuffer_ptr, uint32_t frontb
   // latched on the paths below that return early.
   native_residency_->LatchFrameModeAtSwap();
 
+  if (draw_census::HasPendingRequest() ||
+      (draw_census_ && draw_census_->HasOpenCapture())) {
+    if (!draw_census_) {
+      draw_census_ = std::make_unique<draw_census::Recorder>();
+    }
+    uint32_t census_mode_flags = 0;
+    if (native_residency_->native_frame()) {
+      census_mode_flags |= draw_census::kModeNativeFrame;
+    }
+    if (REXCVAR_GET(gpu_native_index)) {
+      census_mode_flags |= draw_census::kModeNativeIndex;
+    }
+    if (REXCVAR_GET(gpu_native_vertex)) {
+      census_mode_flags |= draw_census::kModeNativeVertex;
+    }
+    if (REXCVAR_GET(gpu_cp_fastpath)) {
+      census_mode_flags |= draw_census::kModeCpFastpath;
+    }
+    if (REXCVAR_GET(gpu_sampler_set_reuse)) {
+      census_mode_flags |= draw_census::kModeSamplerSetReuse;
+    }
+    if (REXCVAR_GET(gpu_texture_request_reuse)) {
+      census_mode_flags |= draw_census::kModeTextureRequestReuse;
+    }
+    if (REXCVAR_GET(trace_gpu_stream)) {
+      census_mode_flags |= draw_census::kModeTraceGpuStream;
+    }
+    const bool census_was_open = draw_census_->HasOpenCapture();
+    draw_census_->OnSwap(
+        kernel_state_ ? kernel_state_->title_id() : 0, draw_census_swap_ordinal_, counter(),
+        GetCurrentFrame(), GetCurrentSubmission(), census_mode_flags,
+        rex::perf::GetSnapshotCounter(rex::perf::CounterId::kDrawCalls));
+    if (!census_was_open && draw_census_->IsRecording()) {
+      std::memset(draw_census_float_dirty_, 0, sizeof(draw_census_float_dirty_));
+      std::memset(draw_census_fetch_dirty_, 0, sizeof(draw_census_fetch_dirty_));
+      draw_census_boolloop_dirty_writes_ = 0;
+    }
+  }
+
   if (!graphics_system_)
     return;
   ui::Presenter* presenter = graphics_system_->presenter();
@@ -3694,6 +3831,18 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     return IssueCopy();
   }
 
+  draw_census::Recorder* census =
+      draw_census_ && draw_census_->IsRecording() ? draw_census_.get() : nullptr;
+  CensusDrawAttempt census_attempt(census);
+  draw_census::DrawRecord census_draw{};
+  uint32_t census_vertex_bindings_seen = 0;
+  if (census) {
+    census_draw.vk_submission = GetCurrentSubmission();
+    census_draw.flags = draw_census::kDrawVizQueryStateUnavailable;
+    census_draw.edram_mode = uint8_t(edram_mode);
+    census_draw.msaa_samples = uint8_t(regs.Get<reg::RB_SURFACE_INFO>().msaa_samples);
+  }
+
   // Suppression oracle. Diagnostic only, never a rendering mode: dropping the
   // draw here skips the primitive processor, every shared memory request and
   // the whole Vulkan issue path, which measures the frame rate ceiling of any
@@ -3718,6 +3867,9 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
   }
   pipeline_cache_->AnalyzeShaderUcode(*vertex_shader);
   bool memexport_used_vertex = vertex_shader->memexport_eM_written() != 0;
+  if (census && memexport_used_vertex) {
+    census_draw.flags |= draw_census::kDrawMemexportVertex;
+  }
   if (memexport_used_vertex) {
     if (!device_properties.vertexPipelineStoresAndAtomics) {
       REXGPU_ERROR(
@@ -3758,6 +3910,17 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     }
   }
   bool memexport_used_pixel = pixel_shader && (pixel_shader->memexport_eM_written() != 0);
+  if (census) {
+    if (memexport_used_pixel) {
+      census_draw.flags |= draw_census::kDrawMemexportPixel;
+    }
+    if (is_rasterization_done) {
+      census_draw.flags |= draw_census::kDrawRasterizationDone;
+    }
+    if (pixel_shader) {
+      census_draw.flags |= draw_census::kDrawPixelShaderNeeded;
+    }
+  }
   if (memexport_used_pixel) {
     if (!device_properties.fragmentStoresAndAtomics) {
       REXGPU_ERROR(
@@ -3812,6 +3975,39 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       // Nothing to draw.
       return true;
     }
+    if (census) {
+      census_draw.guest_prim_type = uint8_t(primitive_processing_result.guest_primitive_type);
+      census_draw.host_prim_type = uint8_t(primitive_processing_result.host_primitive_type);
+      census_draw.host_vs_type = uint8_t(primitive_processing_result.host_vertex_shader_type);
+      census_draw.index_buffer_type = uint8_t(primitive_processing_result.index_buffer_type);
+      census_draw.index_format = uint8_t(primitive_processing_result.host_index_format);
+      census_draw.index_endian = uint8_t(primitive_processing_result.host_shader_index_endian);
+      census_draw.guest_vertex_count = primitive_processing_result.guest_draw_vertex_count;
+      census_draw.host_vertex_count = primitive_processing_result.host_draw_vertex_count;
+      census_draw.guest_index_base = primitive_processing_result.guest_index_base;
+      census_draw.guest_index_bytes =
+          index_buffer_info
+              ? uint32_t(std::min<size_t>(index_buffer_info->length, UINT32_MAX))
+              : 0;
+      if (primitive_processing_result.index_buffer_type !=
+              PrimitiveProcessor::ProcessedIndexBufferType::kNone &&
+          primitive_processing_result.index_buffer_type !=
+              PrimitiveProcessor::ProcessedIndexBufferType::kHostBuiltinForAuto) {
+        census_draw.flags |= draw_census::kDrawIndexed;
+      }
+      if (primitive_processing_result.host_primitive_reset_enabled) {
+        census_draw.flags |= draw_census::kDrawPrimitiveReset;
+      }
+      if (native_residency_->WasIndexNativeForDraw()) {
+        census_draw.flags |= draw_census::kDrawIndexNative;
+      }
+      if ((census_draw.flags & draw_census::kDrawIndexed) &&
+          !native_residency_->LastRequestedRangeHash(
+              census_draw.guest_index_base, census_draw.guest_index_bytes,
+              &census_draw.index_content_hash)) {
+        census_draw.flags |= draw_census::kDrawIndexHashUnavailable;
+      }
+    }
     if (primitive_processing_result.host_primitive_type == xenos::PrimitiveType::kTriangleFan) {
       // Vulkan uses the same fan-to-list conversion policy as D3D12.
       REXGPU_ERROR(
@@ -3854,6 +4050,16 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
                                                   pixel_shader_translation)) {
       return draw_fail("shader_translation");
     }
+    if (census) {
+      census_draw.vs_hash = vertex_shader->ucode_data_hash();
+      census_draw.vs_mod = vertex_shader_modification.value;
+      census_draw.ps_hash = pixel_shader ? pixel_shader->ucode_data_hash() : 0;
+      census_draw.ps_mod = pixel_shader ? pixel_shader_modification.value : 0;
+      census->RecordShader(MakeCensusShaderRecord(*vertex_shader));
+      if (pixel_shader) {
+        census->RecordShader(MakeCensusShaderRecord(*pixel_shader));
+      }
+    }
 
     // Obtain the samplers. Note that the bindings don't depend on the shader
     // modification, so if on the second iteration of this loop it becomes
@@ -3945,6 +4151,39 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     CheckSubmissionFenceAndDeviceLoss(sampler_overflow_await_submission);
   }
 
+  if (census) {
+    const auto& sampler_bindings_vs = vertex_shader->GetSamplerBindingsAfterTranslation();
+    const size_t sampler_count_vs = sampler_bindings_vs.size();
+    const size_t sampler_count_ps =
+        pixel_shader ? pixel_shader->GetSamplerBindingsAfterTranslation().size() : 0;
+    census_draw.sampler_count_vs = uint8_t(std::min<size_t>(sampler_count_vs, UINT8_MAX));
+    census_draw.sampler_count_ps = uint8_t(std::min<size_t>(sampler_count_ps, UINT8_MAX));
+    size_t census_sampler_index = 0;
+    auto record_samplers = [&](const VulkanShader* shader,
+                               const std::vector<std::pair<VulkanTextureCache::SamplerParameters,
+                                                           VkSampler>>& samplers,
+                               uint8_t stage) {
+      if (!shader) {
+        return;
+      }
+      const auto& bindings = shader->GetSamplerBindingsAfterTranslation();
+      for (size_t i = 0; i < bindings.size() && i < samplers.size(); ++i) {
+        if (census_sampler_index < rex::countof(census_draw.samplers)) {
+          draw_census::SamplerRecord& sampler = census_draw.samplers[census_sampler_index];
+          sampler.fetch_constant = uint8_t(bindings[i].fetch_constant);
+          sampler.stage = stage;
+          sampler.parameters = samplers[i].first.value;
+        }
+        ++census_sampler_index;
+      }
+    };
+    record_samplers(vertex_shader, current_samplers_vertex_, 0);
+    record_samplers(pixel_shader, current_samplers_pixel_, 1);
+    if (census_sampler_index > rex::countof(census_draw.samplers)) {
+      census_draw.flags |= draw_census::kDrawSamplerOverflow;
+    }
+  }
+
   uint32_t normalized_color_mask =
       pixel_shader ? draw_util::GetNormalizedColorMask(regs, pixel_shader->writes_color_targets())
                    : 0;
@@ -3956,6 +4195,29 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       vertex_shader->GetUsedTextureMaskAfterTranslation() |
       (pixel_shader != nullptr ? pixel_shader->GetUsedTextureMaskAfterTranslation() : 0);
   texture_cache_->RequestTextures(used_texture_mask);
+  if (census) {
+    census_draw.used_texture_mask = used_texture_mask;
+    census_draw.texture_count = uint8_t(std::min<uint32_t>(std::popcount(used_texture_mask),
+                                                           UINT8_MAX));
+    uint32_t texture_bits = used_texture_mask;
+    uint32_t census_texture_index = 0;
+    uint32_t fetch_constant;
+    while (rex::bit_scan_forward(texture_bits, &fetch_constant)) {
+      texture_bits &= ~(UINT32_C(1) << fetch_constant);
+      if (census_texture_index < rex::countof(census_draw.textures)) {
+        draw_census::TextureRecord& texture = census_draw.textures[census_texture_index];
+        texture.fetch_constant = uint8_t(fetch_constant);
+        texture.swizzled_signs =
+            texture_cache_->GetActiveTextureSwizzledSigns(fetch_constant);
+        texture.host_swizzle = texture_cache_->GetActiveTextureHostSwizzle(fetch_constant);
+        texture_cache_->GetActiveTextureCensusKey(fetch_constant, texture.key);
+      }
+      ++census_texture_index;
+    }
+    if (census_texture_index > rex::countof(census_draw.textures)) {
+      census_draw.flags |= draw_census::kDrawTextureOverflow;
+    }
+  }
 
   const VulkanPipelineCache::PipelineLayoutProvider* pipeline_layout_provider;
   // Set up the render targets - this may perform dispatches and draws.
@@ -3976,6 +4238,10 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
                                           pipeline, pipeline_layout_provider, &pipeline_handle)) {
     return draw_fail("configure_pipeline");
   }
+  if (census) {
+    census_draw.pipeline_desc_hash = pipeline_cache_->last_pipeline_description_hash();
+    census_draw.render_pass_key = render_target_cache_->last_update_render_pass_key().key;
+  }
   bool pipeline_is_placeholder = false;
   // Reload the current handle state to observe async hot-swap completions that
   // may happen between pipeline configuration and binding.
@@ -4107,7 +4373,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       uint32_t vfetch_index = i * 32 + j;
       uint64_t vfetch_bit = uint64_t(1) << (vfetch_index & 63);
       bool vfetch_cached = (vertex_buffers_in_sync_[vfetch_index >> 6] & vfetch_bit) != 0;
-      if (vfetch_cached && !native_residency_->VerifyBypassesCallerCache()) {
+      if (!census && vfetch_cached && !native_residency_->VerifyBypassesCallerCache()) {
         continue;
       }
       xenos::xe_gpu_vertex_fetch_t vfetch_constant = regs.GetVertexFetch(vfetch_index);
@@ -4132,6 +4398,28 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       }
       uint32_t vfetch_address_bytes = vfetch_constant.address << 2;
       uint32_t vfetch_size_bytes = vfetch_constant.size << 2;
+      draw_census::VertexBindingRecord* census_binding = nullptr;
+      if (census) {
+        if (census_vertex_bindings_seen < rex::countof(census_draw.vertex_bindings)) {
+          census_binding = &census_draw.vertex_bindings[census_vertex_bindings_seen];
+          census_binding->fetch_constant = uint8_t(vfetch_index);
+          census_binding->endian = uint8_t(vfetch_constant.endian);
+          census_binding->address_bytes = vfetch_address_bytes;
+          census_binding->size_bytes = vfetch_size_bytes;
+          for (const Shader::VertexBinding& binding : vertex_shader->vertex_bindings()) {
+            if (binding.fetch_constant == vfetch_index) {
+              census_binding->stride_words = binding.stride_words;
+              break;
+            }
+          }
+          census_binding->hash_valid = native_residency_->LastRequestedRangeHash(
+              vfetch_address_bytes, vfetch_size_bytes, &census_binding->content_hash);
+        }
+        ++census_vertex_bindings_seen;
+      }
+      if (vfetch_cached && !native_residency_->VerifyBypassesCallerCache()) {
+        continue;
+      }
       if (REXCVAR_GET(gpu_native_stats)) {
         native_telemetry::NoteVertexRange(vfetch_address_bytes, vfetch_size_bytes);
       }
@@ -4141,6 +4429,11 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       if (native_residency_->IsVertexResidencyAllowedForDraw()) {
         NativeResidency::VertexRangeResult result = native_residency_->RequestVertexRange(
             *memory_, *shared_memory_, vfetch_address_bytes, vfetch_size_bytes, vfetch_cached);
+        if (census_binding) {
+          census_binding->residency_result = uint8_t(result);
+          census_binding->hash_valid = native_residency_->LastRequestedRangeHash(
+              vfetch_address_bytes, vfetch_size_bytes, &census_binding->content_hash);
+        }
         if (result == NativeResidency::VertexRangeResult::kCallerCacheHit) {
           // The fetch constant has not moved since this buffer was last made
           // resident, so neither path has anything to do. RequestVertexRange
@@ -4162,6 +4455,10 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
         vertex_buffers_in_sync_[vfetch_index >> 6] |= vfetch_bit;
         continue;
       }
+      if (census_binding) {
+        census_binding->residency_result =
+            uint8_t(NativeResidency::VertexRangeResult::kFallback);
+      }
       PERF_counter_add(kRequestRangeVertexBytes, vfetch_size_bytes);
       if (!shared_memory_->RequestRange(vfetch_address_bytes, vfetch_size_bytes)) {
         REXGPU_ERROR(
@@ -4175,6 +4472,13 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       vertex_buffers_in_sync_[vfetch_index >> 6] |= vfetch_bit;
     }
   }
+  if (census) {
+    census_draw.vertex_binding_count =
+        uint8_t(std::min<uint32_t>(census_vertex_bindings_seen, UINT8_MAX));
+    if (census_vertex_bindings_seen > rex::countof(census_draw.vertex_bindings)) {
+      census_draw.flags |= draw_census::kDrawVertexBindingOverflow;
+    }
+  }
 
   // Every residency decision for this draw has been made by now - the index
   // buffer inside PrimitiveProcessor::Process above, the vertex buffers in the
@@ -4271,6 +4575,72 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
       render_target_cache_->last_update_render_pass(),
       render_target_cache_->last_update_framebuffer());
 
+  auto commit_census_draw = [&]() {
+    if (!census) {
+      return;
+    }
+    census_draw.vk_submission = GetCurrentSubmission();
+    census_draw.rb_surface_info = regs[XE_GPU_REG_RB_SURFACE_INFO];
+    census_draw.rb_modecontrol = regs[XE_GPU_REG_RB_MODECONTROL];
+    census_draw.rb_colorcontrol = regs[XE_GPU_REG_RB_COLORCONTROL];
+    census_draw.rb_depthcontrol_normalized = normalized_depth_control.value;
+    census_draw.rb_color_mask_normalized = normalized_color_mask;
+    for (uint32_t i = 0; i < 4; ++i) {
+      census_draw.rb_color_info[i] = regs[reg::RB_COLOR_INFO::rt_register_indices[i]];
+      census_draw.rb_blendcontrol[i] = regs[reg::RB_BLENDCONTROL::rt_register_indices[i]];
+    }
+    census_draw.rb_depth_info = regs[XE_GPU_REG_RB_DEPTH_INFO];
+    census_draw.rb_stencilrefmask = regs[XE_GPU_REG_RB_STENCILREFMASK];
+    census_draw.rb_stencilrefmask_bf = regs[XE_GPU_REG_RB_STENCILREFMASK_BF];
+    census_draw.rb_alpha_ref_bits = regs[XE_GPU_REG_RB_ALPHA_REF];
+    census_draw.pa_su_sc_mode_cntl = regs[XE_GPU_REG_PA_SU_SC_MODE_CNTL];
+    census_draw.pa_cl_vte_cntl = regs[XE_GPU_REG_PA_CL_VTE_CNTL];
+    census_draw.pa_sc_window_scissor_tl = regs[XE_GPU_REG_PA_SC_WINDOW_SCISSOR_TL];
+    census_draw.pa_sc_window_scissor_br = regs[XE_GPU_REG_PA_SC_WINDOW_SCISSOR_BR];
+    census_draw.sq_program_cntl = regs[XE_GPU_REG_SQ_PROGRAM_CNTL];
+    census_draw.sq_context_misc = regs[XE_GPU_REG_SQ_CONTEXT_MISC];
+    for (uint32_t i = 0; i < 2; ++i) {
+      census_draw.vp_xy_offset[i] = float(viewport_info.xy_offset[i]);
+      census_draw.vp_xy_extent[i] = float(viewport_info.xy_extent[i]);
+    }
+    census_draw.vp_z_min = viewport_info.z_min;
+    census_draw.vp_z_max = viewport_info.z_max;
+    draw_util::Scissor census_scissor;
+    draw_util::GetScissor(regs, census_scissor);
+    std::memcpy(census_draw.sc_offset, census_scissor.offset, sizeof(census_draw.sc_offset));
+    std::memcpy(census_draw.sc_extent, census_scissor.extent, sizeof(census_draw.sc_extent));
+    std::memcpy(census_draw.float_dirty, draw_census_float_dirty_,
+                sizeof(census_draw.float_dirty));
+    std::memcpy(census_draw.fetch_dirty, draw_census_fetch_dirty_,
+                sizeof(census_draw.fetch_dirty));
+    census_draw.boolloop_dirty_writes = draw_census_boolloop_dirty_writes_;
+
+    const uint32_t* vs_constants = &regs[XE_GPU_REG_SHADER_CONSTANT_000_X];
+    const uint32_t* ps_constants = &regs[XE_GPU_REG_SHADER_CONSTANT_256_X];
+    constexpr size_t kConstantBlockBytes = 256 * 4 * sizeof(uint32_t);
+    census_draw.vs_const_block_hash = XXH3_64bits(vs_constants, kConstantBlockBytes);
+    census_draw.ps_const_block_hash = XXH3_64bits(ps_constants, kConstantBlockBytes);
+    const Shader::ConstantRegisterMap& vs_map = vertex_shader->constant_register_map();
+    const Shader::ConstantRegisterMap* ps_map =
+        pixel_shader ? &pixel_shader->constant_register_map() : nullptr;
+    for (uint32_t i = 0; i < 256; ++i) {
+      if (vs_map.float_bitmap[i >> 6] & (UINT64_C(1) << (i & 63))) {
+        census_draw.vs_reg_hash[i] = uint32_t(XXH3_64bits(vs_constants + i * 4, 16));
+      }
+      if (ps_map && (ps_map->float_bitmap[i >> 6] & (UINT64_C(1) << (i & 63)))) {
+        census_draw.ps_reg_hash[i] = uint32_t(XXH3_64bits(ps_constants + i * 4, 16));
+      }
+    }
+
+    const bool recorded = census->RecordDraw(census_draw);
+    census_attempt.Commit();
+    if (recorded) {
+      std::memset(draw_census_float_dirty_, 0, sizeof(draw_census_float_dirty_));
+      std::memset(draw_census_fetch_dirty_, 0, sizeof(draw_census_fetch_dirty_));
+      draw_census_boolloop_dirty_writes_ = 0;
+    }
+  };
+
   // Draw.
   if (primitive_processing_result.index_buffer_type ==
           PrimitiveProcessor::ProcessedIndexBufferType::kNone ||
@@ -4280,6 +4650,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
     // incremented them - the overlay showed "Draw: 0" for every Vulkan run.
     // Mirrors d3d12/command_processor.cpp exactly, at the same point in the
     // sequence (after the barriers, on the instruction that issues the draw).
+    commit_census_draw();
     PROFILE_DRAW_CALL();
     PROFILE_VERTICES(primitive_processing_result.host_draw_vertex_count);
     deferred_command_buffer_.CmdVkDraw(primitive_processing_result.host_draw_vertex_count, 1, 0, 0);
@@ -4313,6 +4684,7 @@ bool VulkanCommandProcessor::IssueDraw(xenos::PrimitiveType prim_type, uint32_t
         primitive_processing_result.host_index_format == xenos::IndexFormat::kInt16
             ? VK_INDEX_TYPE_UINT16
             : VK_INDEX_TYPE_UINT32);
+    commit_census_draw();
     PROFILE_DRAW_CALL();
     PROFILE_VERTICES(primitive_processing_result.host_draw_vertex_count);
     deferred_command_buffer_.CmdVkDrawIndexed(primitive_processing_result.host_draw_vertex_count, 1,
@@ -4546,6 +4918,28 @@ bool VulkanCommandProcessor::IssueDraw_MemexportReadbackFastPath(uint32_t total_
   return true;
 }
 
+void VulkanCommandProcessor::RecordDrawCensusCopy(uint32_t written_address,
+                                                  uint32_t written_length,
+                                                  bool readback_path) {
+  if (!draw_census_ || !draw_census_->IsRecording()) {
+    return;
+  }
+  const RegisterFile& regs = *register_file_;
+  draw_census::CopyRecord record{};
+  record.rb_copy_control = regs[XE_GPU_REG_RB_COPY_CONTROL];
+  record.rb_copy_dest_info = regs[XE_GPU_REG_RB_COPY_DEST_INFO];
+  record.rb_copy_dest_base = regs[XE_GPU_REG_RB_COPY_DEST_BASE];
+  record.rb_copy_dest_pitch = regs[XE_GPU_REG_RB_COPY_DEST_PITCH];
+  record.written_address = written_address;
+  record.written_length = written_length;
+  record.rb_surface_info = regs[XE_GPU_REG_RB_SURFACE_INFO];
+  record.rb_color_info0 = regs[XE_GPU_REG_RB_COLOR_INFO];
+  record.rb_depth_info = regs[XE_GPU_REG_RB_DEPTH_INFO];
+  record.rb_modecontrol = regs[XE_GPU_REG_RB_MODECONTROL];
+  record.readback_path = readback_path;
+  draw_census_->RecordCopy(record);
+}
+
 bool VulkanCommandProcessor::IssueCopy() {
 #if XE_GPU_FINE_GRAINED_DRAW_SCOPES
   SCOPE_profile_cpu_f("gpu");
@@ -4561,6 +4955,7 @@ bool VulkanCommandProcessor::IssueCopy() {
     bool resolved = render_target_cache_->Resolve(*memory_, *shared_memory_, *texture_cache_,
                                                   written_address, written_length);
     if (resolved) {
+      RecordDrawCensusCopy(written_address, written_length, false);
       // Unconditionally, so that the exclusion set is already complete if the
       // native path is switched on later in the run.
       native_residency_->NoteResolveDestination(written_address, written_length);
@@ -4585,6 +4980,7 @@ bool VulkanCommandProcessor::IssueCopy_ReadbackResolvePath() {
     return false;
   }
   native_residency_->NoteResolveDestination(written_address, written_length);
+  RecordDrawCensusCopy(written_address, written_length, true);
   if (REXCVAR_GET(gpu_native_stats)) {
     native_telemetry::NoteResolveDestination(written_address, written_length);
   }
diff --git a/src/graphics/vulkan/draw_census.cc b/src/graphics/vulkan/draw_census.cc
new file mode 100644
index 0000000..5523fa7
--- /dev/null
+++ b/src/graphics/vulkan/draw_census.cc
@@ -0,0 +1,333 @@
+/**
+ ******************************************************************************
+ * ReXGlue : Xenos GPU plugin - single-frame draw census                     *
+ ******************************************************************************
+ * Released under the BSD license - see LICENSE in the root for more details. *
+ */
+
+#include "draw_census.h"
+
+#include <algorithm>
+#include <atomic>
+#include <chrono>
+#include <cstdio>
+#include <cstring>
+#include <mutex>
+#include <new>
+
+#include <rex/cvar.h>
+#include <rex/logging.h>
+#include <rex/platform.h>
+#include <rex/version.h>
+
+#if REX_PLATFORM_WIN32
+#include <process.h>
+#else
+#include <unistd.h>
+#endif
+
+REXCVAR_DEFINE_STRING(gpu_census_dir, "", "GPU/Vulkan",
+                      "Directory for one-frame draw census captures");
+REXCVAR_DEFINE_STRING(gpu_census_tag, "", "GPU/Vulkan",
+                      "Build identifier stored in draw census headers");
+REXCVAR_DEFINE_INT32(gpu_census_capacity_draws, 4096, "GPU/Vulkan",
+                     "Maximum draw records in one draw census capture");
+
+// Defined by rexcore. The GPU plugin reaches it through the import library.
+REXCVAR_DECLARE(std::string, perf_log_csv);
+
+namespace rex::graphics::vulkan::draw_census {
+namespace {
+
+std::atomic<uint32_t> g_requests_pending{0};
+std::atomic<uint32_t> g_requested{0};
+std::atomic<uint32_t> g_started{0};
+std::atomic<uint32_t> g_completed{0};
+std::atomic<uint32_t> g_dropped{0};
+std::atomic<uint32_t> g_truncated{0};
+std::mutex g_last_path_mutex;
+std::string g_last_path = "-";
+
+uint32_t ProcessId() {
+#if REX_PLATFORM_WIN32
+  return uint32_t(_getpid());
+#else
+  return uint32_t(getpid());
+#endif
+}
+
+void SetLastPath(const std::filesystem::path& path) {
+  std::lock_guard<std::mutex> lock(g_last_path_mutex);
+  g_last_path = path.string();
+}
+
+void LogStatus() {
+  std::string last_path;
+  {
+    std::lock_guard<std::mutex> lock(g_last_path_mutex);
+    last_path = g_last_path;
+  }
+  REXGPU_INFO(
+      "draw-census: requested={} started={} completed={} dropped={} truncated={} last_path={}",
+      g_requested.load(std::memory_order_relaxed), g_started.load(std::memory_order_relaxed),
+      g_completed.load(std::memory_order_relaxed), g_dropped.load(std::memory_order_relaxed),
+      g_truncated.load(std::memory_order_relaxed), last_path);
+}
+
+void RequestCapture() {
+  g_requested.fetch_add(1, std::memory_order_relaxed);
+  g_requests_pending.fetch_add(1, std::memory_order_release);
+}
+
+}  // namespace
+
+REXCVAR_DEFINE_COMMAND(gpu_census_capture, RequestCapture, "GPU/Vulkan",
+                       "Capture the next complete guest GPU frame to a .dcen file");
+REXCVAR_DEFINE_COMMAND(gpu_census_status, LogStatus, "GPU/Vulkan",
+                       "Log draw census request and completion counters");
+
+bool HasPendingRequest() { return g_requests_pending.load(std::memory_order_acquire) != 0; }
+
+bool Recorder::Append(const void* data, size_t size, bool reserve_trailer) {
+  const size_t trailer_reserve = reserve_trailer ? sizeof(TrailerRecord) : 0;
+  if (size > max_buffer_bytes_ || buffer_.size() > max_buffer_bytes_ - size ||
+      buffer_.size() + size > max_buffer_bytes_ - trailer_reserve) {
+    MarkTruncated();
+    return false;
+  }
+  const size_t offset = buffer_.size();
+  buffer_.resize(offset + size);
+  std::memcpy(buffer_.data() + offset, data, size);
+  return true;
+}
+
+void Recorder::MarkTruncated() {
+  if (!capture_truncated_) {
+    capture_truncated_ = true;
+    g_truncated.fetch_add(1, std::memory_order_relaxed);
+  }
+}
+
+bool Recorder::Start(uint32_t title_id, uint64_t guest_swap_ordinal,
+                     uint64_t guest_vblank_counter, uint64_t vk_frame_current,
+                     uint64_t vk_submission, uint32_t mode_flags) {
+  const int32_t configured_capacity = REXCVAR_GET(gpu_census_capacity_draws);
+  capacity_draws_ = uint32_t(std::max(configured_capacity, 1));
+  max_buffer_bytes_ = sizeof(HeaderRecord) +
+                      size_t(capacity_draws_) * sizeof(DrawRecord) +
+                      size_t(1) * 1024 * 1024;
+
+  buffer_.clear();
+  shaders_seen_.clear();
+  try {
+    buffer_.reserve(max_buffer_bytes_);
+    shaders_seen_.reserve(256);
+  } catch (const std::bad_alloc&) {
+    buffer_.clear();
+    shaders_seen_.clear();
+    g_dropped.fetch_add(1, std::memory_order_relaxed);
+    REXGPU_ERROR("draw-census: unable to allocate {} bytes for capture",
+                 max_buffer_bytes_);
+    return false;
+  }
+
+  capture_truncated_ = false;
+  event_ordinal_ = 0;
+  draws_recorded_ = 0;
+  draws_skipped_ = 0;
+  copies_ = 0;
+  shaders_ = 0;
+  overflow_vertex_ = 0;
+  overflow_texture_ = 0;
+  overflow_sampler_ = 0;
+  guest_swap_ordinal_open_ = guest_swap_ordinal;
+
+  std::filesystem::path directory = REXCVAR_GET(gpu_census_dir);
+  if (directory.empty()) {
+    const std::filesystem::path perf_path = REXCVAR_GET(perf_log_csv);
+    directory = perf_path.parent_path();
+  }
+  char file_name[64];
+  std::snprintf(file_name, sizeof(file_name), "census_%08x_%08llu.dcen", title_id,
+                static_cast<unsigned long long>(guest_swap_ordinal));
+  output_path_ = directory / file_name;
+
+  HeaderRecord header{};
+  header.prefix = {kRecordHeader, uint16_t(sizeof(HeaderRecord)), 0};
+  std::memcpy(header.magic, "THP8DCEN", sizeof(header.magic));
+  header.schema_version = kSchemaVersion;
+  header.header_size = sizeof(HeaderRecord);
+  std::snprintf(header.sdk_version, sizeof(header.sdk_version), "%s", REXGLUE_VERSION_STRING);
+  std::snprintf(header.census_tag, sizeof(header.census_tag), "%s",
+                REXCVAR_GET(gpu_census_tag).c_str());
+  header.title_id = title_id;
+  header.pid = ProcessId();
+  header.unix_time_ns = uint64_t(std::chrono::duration_cast<std::chrono::nanoseconds>(
+                                     std::chrono::system_clock::now().time_since_epoch())
+                                     .count());
+  header.guest_swap_ordinal = guest_swap_ordinal;
+  header.vk_frame_current = vk_frame_current;
+  header.vk_submission = vk_submission;
+  header.mode_flags = mode_flags;
+  header.capacity_draws = capacity_draws_;
+  header.draw_record_size = sizeof(DrawRecord);
+  header.shader_record_size = sizeof(ShaderRecord);
+  header.copy_record_size = sizeof(CopyRecord);
+  header.trailer_size = sizeof(TrailerRecord);
+  header.reserved[0] = uint32_t(guest_vblank_counter);
+  header.reserved[1] = uint32_t(guest_vblank_counter >> 32);
+
+  recording_ = true;
+  if (!Append(&header, sizeof(header))) {
+    recording_ = false;
+    g_dropped.fetch_add(1, std::memory_order_relaxed);
+    return false;
+  }
+  g_started.fetch_add(1, std::memory_order_relaxed);
+  return true;
+}
+
+void Recorder::Complete(uint64_t guest_swap_ordinal_close, int64_t draw_calls_snapshot) {
+  const uint32_t completed = g_completed.fetch_add(1, std::memory_order_relaxed) + 1;
+
+  TrailerRecord trailer{};
+  trailer.prefix = {kRecordTrailer, uint16_t(sizeof(TrailerRecord)), event_ordinal_};
+  trailer.draws_recorded = draws_recorded_;
+  trailer.draws_skipped = draws_skipped_;
+  trailer.copies = copies_;
+  trailer.shaders = shaders_;
+  trailer.requested = g_requested.load(std::memory_order_relaxed);
+  trailer.started = g_started.load(std::memory_order_relaxed);
+  trailer.completed = completed;
+  trailer.dropped = g_dropped.load(std::memory_order_relaxed);
+  trailer.truncated = g_truncated.load(std::memory_order_relaxed);
+  trailer.overflow_vertex = overflow_vertex_;
+  trailer.overflow_texture = overflow_texture_;
+  trailer.overflow_sampler = overflow_sampler_;
+  trailer.draw_calls_snapshot = draw_calls_snapshot;
+  trailer.bytes_written = buffer_.size() + sizeof(TrailerRecord);
+  trailer.guest_swap_ordinal_close = guest_swap_ordinal_close;
+  Append(&trailer, sizeof(trailer), false);
+
+  std::error_code directory_error;
+  if (!output_path_.parent_path().empty()) {
+    std::filesystem::create_directories(output_path_.parent_path(), directory_error);
+  }
+  bool wrote_file = false;
+  if (!directory_error) {
+    if (std::FILE* file = std::fopen(output_path_.string().c_str(), "wb")) {
+      wrote_file = std::fwrite(buffer_.data(), 1, buffer_.size(), file) == buffer_.size();
+      wrote_file = std::fclose(file) == 0 && wrote_file;
+    }
+  }
+  SetLastPath(output_path_);
+  if (!wrote_file) {
+    REXGPU_ERROR("draw-census: failed to write {}", output_path_.string());
+  }
+
+  REXGPU_INFO(
+      "draw-census: completed swap={} draws={} skipped={} copies={} shaders={} bytes={} "
+      "truncated={} path={}",
+      guest_swap_ordinal_open_, draws_recorded_, draws_skipped_, copies_, shaders_,
+      buffer_.size(), capture_truncated_ ? 1 : 0, output_path_.string());
+
+  recording_ = false;
+  capture_truncated_ = false;
+  buffer_.clear();
+  buffer_.shrink_to_fit();
+  shaders_seen_.clear();
+  shaders_seen_.shrink_to_fit();
+}
+
+void Recorder::OnSwap(uint32_t title_id, uint64_t guest_swap_ordinal,
+                      uint64_t guest_vblank_counter, uint64_t vk_frame_current,
+                      uint64_t vk_submission, uint32_t mode_flags,
+                      int64_t draw_calls_snapshot) {
+  if (recording_) {
+    // Requests made while the capture frame was open are deliberately not
+    // queued behind it: each command means exactly one next frame.
+    const uint32_t requests_during_capture =
+        g_requests_pending.exchange(0, std::memory_order_acq_rel);
+    if (requests_during_capture) {
+      g_dropped.fetch_add(requests_during_capture, std::memory_order_relaxed);
+    }
+    Complete(guest_swap_ordinal, draw_calls_snapshot);
+    return;
+  }
+
+  const uint32_t requests = g_requests_pending.exchange(0, std::memory_order_acq_rel);
+  if (!requests) {
+    return;
+  }
+  if (requests > 1) {
+    g_dropped.fetch_add(requests - 1, std::memory_order_relaxed);
+  }
+  Start(title_id, guest_swap_ordinal, guest_vblank_counter, vk_frame_current,
+        vk_submission, mode_flags);
+}
+
+bool Recorder::RecordShader(ShaderRecord record) {
+  if (!IsRecording()) {
+    return false;
+  }
+  const std::pair<uint64_t, uint8_t> identity{record.ucode_hash, record.shader_type};
+  if (std::find(shaders_seen_.begin(), shaders_seen_.end(), identity) != shaders_seen_.end()) {
+    return true;
+  }
+  record.prefix = {kRecordShader, uint16_t(sizeof(ShaderRecord)), event_ordinal_};
+  if (!Append(&record, sizeof(record))) {
+    return false;
+  }
+  shaders_seen_.push_back(identity);
+  ++shaders_;
+  return true;
+}
+
+bool Recorder::RecordDraw(DrawRecord record) {
+  if (!IsRecording()) {
+    return false;
+  }
+  if (draws_recorded_ >= capacity_draws_) {
+    MarkTruncated();
+    return false;
+  }
+  record.prefix = {kRecordDraw, uint16_t(sizeof(DrawRecord)), event_ordinal_};
+  record.draw_ordinal = draws_recorded_;
+  if (!Append(&record, sizeof(record))) {
+    return false;
+  }
+  if (record.flags & kDrawVertexBindingOverflow) {
+    ++overflow_vertex_;
+  }
+  if (record.flags & kDrawTextureOverflow) {
+    ++overflow_texture_;
+  }
+  if (record.flags & kDrawSamplerOverflow) {
+    ++overflow_sampler_;
+  }
+  ++draws_recorded_;
+  ++event_ordinal_;
+  return true;
+}
+
+bool Recorder::RecordCopy(CopyRecord record) {
+  if (!IsRecording()) {
+    return false;
+  }
+  record.prefix = {kRecordCopy, uint16_t(sizeof(CopyRecord)), event_ordinal_};
+  record.draws_before = draws_recorded_;
+  if (!Append(&record, sizeof(record))) {
+    return false;
+  }
+  ++copies_;
+  ++event_ordinal_;
+  return true;
+}
+
+void Recorder::NoteDrawSkipped() {
+  if (IsRecording()) {
+    ++draws_skipped_;
+  }
+}
+
+}  // namespace rex::graphics::vulkan::draw_census
diff --git a/src/graphics/vulkan/draw_census.h b/src/graphics/vulkan/draw_census.h
new file mode 100644
index 0000000..cf1a7e5
--- /dev/null
+++ b/src/graphics/vulkan/draw_census.h
@@ -0,0 +1,288 @@
+#pragma once
+/**
+ ******************************************************************************
+ * ReXGlue : Xenos GPU plugin - single-frame draw census                     *
+ ******************************************************************************
+ * Released under the BSD license - see LICENSE in the root for more details. *
+ *
+ * Authored instrumentation. The capture contains hashes and layout facts only:
+ * no guest bytes, shader microcode, SPIR-V, textures, or render-target data.
+ */
+
+#include <cstddef>
+#include <cstdint>
+#include <filesystem>
+#include <string>
+#include <utility>
+#include <vector>
+
+namespace rex::graphics::vulkan::draw_census {
+
+constexpr uint32_t kSchemaVersion = 1;
+constexpr uint16_t kRecordHeader = 1;
+constexpr uint16_t kRecordShader = 2;
+constexpr uint16_t kRecordDraw = 3;
+constexpr uint16_t kRecordCopy = 4;
+constexpr uint16_t kRecordTrailer = 5;
+
+constexpr uint32_t kModeNativeFrame = UINT32_C(1) << 0;
+constexpr uint32_t kModeNativeIndex = UINT32_C(1) << 1;
+constexpr uint32_t kModeNativeVertex = UINT32_C(1) << 2;
+constexpr uint32_t kModeCpFastpath = UINT32_C(1) << 3;
+constexpr uint32_t kModeSamplerSetReuse = UINT32_C(1) << 4;
+constexpr uint32_t kModeTextureRequestReuse = UINT32_C(1) << 5;
+constexpr uint32_t kModeTraceGpuStream = UINT32_C(1) << 6;
+
+constexpr uint32_t kDrawIndexed = UINT32_C(1) << 0;
+constexpr uint32_t kDrawMemexportVertex = UINT32_C(1) << 1;
+constexpr uint32_t kDrawMemexportPixel = UINT32_C(1) << 2;
+constexpr uint32_t kDrawRasterizationDone = UINT32_C(1) << 3;
+constexpr uint32_t kDrawPixelShaderNeeded = UINT32_C(1) << 4;
+constexpr uint32_t kDrawPrimitiveReset = UINT32_C(1) << 5;
+constexpr uint32_t kDrawIndexNative = UINT32_C(1) << 6;
+constexpr uint32_t kDrawVertexBindingOverflow = UINT32_C(1) << 7;
+constexpr uint32_t kDrawTextureOverflow = UINT32_C(1) << 8;
+constexpr uint32_t kDrawSamplerOverflow = UINT32_C(1) << 9;
+constexpr uint32_t kDrawVizQueryKilled = UINT32_C(1) << 10;
+// The native index upload path copies without fingerprinting. Keep the frozen
+// field zero and make that absence explicit rather than treating zero as a
+// stable generation.
+constexpr uint32_t kDrawIndexHashUnavailable = UINT32_C(1) << 11;
+// ExecutePacketType3_DRAW_INDX supplies the visibility-query condition above
+// the backend IssueDraw interface, so H2 cannot recover whether it killed this
+// invocation. Bit 10 stays zero and this bit documents the missing fact.
+constexpr uint32_t kDrawVizQueryStateUnavailable = UINT32_C(1) << 12;
+
+#pragma pack(push, 1)
+
+struct RecordPrefix {
+  uint16_t type;
+  uint16_t size_bytes;
+  uint32_t ordinal;
+};
+
+struct HeaderRecord {
+  RecordPrefix prefix;
+  char magic[8];
+  uint32_t schema_version;
+  uint32_t header_size;
+  char sdk_version[64];
+  char census_tag[64];
+  uint32_t title_id;
+  uint32_t pid;
+  uint64_t unix_time_ns;
+  uint64_t guest_swap_ordinal;
+  uint64_t vk_frame_current;
+  uint64_t vk_submission;
+  uint32_t mode_flags;
+  uint32_t capacity_draws;
+  uint16_t draw_record_size;
+  uint16_t shader_record_size;
+  uint16_t copy_record_size;
+  uint16_t trailer_size;
+  // reserved[0..1] is the opening CommandProcessor::counter() value as a
+  // little-endian u64. It counts vblanks as well as swap packets and is
+  // informational only; guest_swap_ordinal is the IssueSwap-only ordinal.
+  uint32_t reserved[8];
+};
+
+struct ShaderRecord {
+  RecordPrefix prefix;
+  uint64_t ucode_hash;
+  uint8_t shader_type;
+  uint8_t memexport;
+  uint16_t vertex_binding_count;
+  uint32_t ucode_dword_count;
+  uint32_t float_count;
+  uint64_t float_bitmap[4];
+  uint32_t bool_bitmap[8];
+  uint32_t loop_bitmap;
+  uint32_t vertex_fetch_bitmap[3];
+  uint32_t texture_mask;
+  uint64_t vertex_layout_hash;
+};
+
+struct VertexBindingRecord {
+  uint8_t fetch_constant;
+  uint8_t endian;
+  uint8_t residency_result;
+  uint8_t hash_valid;
+  uint32_t address_bytes;
+  uint32_t size_bytes;
+  uint32_t stride_words;
+  uint64_t content_hash;
+};
+
+struct TextureRecord {
+  uint8_t fetch_constant;
+  uint8_t swizzled_signs;
+  uint16_t pad;
+  uint32_t host_swizzle;
+  uint8_t key[16];
+};
+
+struct SamplerRecord {
+  uint8_t fetch_constant;
+  uint8_t stage;
+  uint16_t pad;
+  uint32_t parameters;
+};
+
+struct DrawRecord {
+  RecordPrefix prefix;
+  uint32_t draw_ordinal;
+  uint64_t vk_submission;
+  uint32_t flags;
+  uint8_t guest_prim_type;
+  uint8_t host_prim_type;
+  uint8_t host_vs_type;
+  uint8_t index_buffer_type;
+  uint8_t index_format;
+  uint8_t index_endian;
+  uint8_t edram_mode;
+  uint8_t msaa_samples;
+  uint32_t guest_vertex_count;
+  uint32_t host_vertex_count;
+  uint32_t guest_index_base;
+  uint32_t guest_index_bytes;
+  uint64_t index_content_hash;
+  uint64_t vs_hash;
+  uint64_t vs_mod;
+  uint64_t ps_hash;
+  uint64_t ps_mod;
+  uint64_t pipeline_desc_hash;
+  uint32_t render_pass_key;
+  uint32_t rb_surface_info;
+  uint32_t rb_modecontrol;
+  uint32_t rb_colorcontrol;
+  uint32_t rb_depthcontrol_normalized;
+  uint32_t rb_color_mask_normalized;
+  uint32_t rb_color_info[4];
+  uint32_t rb_depth_info;
+  uint32_t rb_blendcontrol[4];
+  uint32_t rb_stencilrefmask;
+  uint32_t rb_stencilrefmask_bf;
+  uint32_t rb_alpha_ref_bits;
+  uint32_t pa_su_sc_mode_cntl;
+  uint32_t pa_cl_vte_cntl;
+  uint32_t pa_sc_window_scissor_tl;
+  uint32_t pa_sc_window_scissor_br;
+  uint32_t sq_program_cntl;
+  uint32_t sq_context_misc;
+  float vp_xy_offset[2];
+  float vp_xy_extent[2];
+  float vp_z_min;
+  float vp_z_max;
+  uint32_t sc_offset[2];
+  uint32_t sc_extent[2];
+  uint64_t float_dirty[8];
+  uint32_t fetch_dirty[6];
+  uint32_t boolloop_dirty_writes;
+  uint64_t vs_const_block_hash;
+  uint64_t ps_const_block_hash;
+  uint32_t vs_reg_hash[256];
+  uint32_t ps_reg_hash[256];
+  uint32_t used_texture_mask;
+  uint8_t vertex_binding_count;
+  uint8_t texture_count;
+  uint8_t sampler_count_vs;
+  uint8_t sampler_count_ps;
+  VertexBindingRecord vertex_bindings[16];
+  TextureRecord textures[16];
+  SamplerRecord samplers[16];
+};
+
+struct CopyRecord {
+  RecordPrefix prefix;
+  uint32_t draws_before;
+  uint32_t rb_copy_control;
+  uint32_t rb_copy_dest_info;
+  uint32_t rb_copy_dest_base;
+  uint32_t rb_copy_dest_pitch;
+  uint32_t written_address;
+  uint32_t written_length;
+  uint32_t rb_surface_info;
+  uint32_t rb_color_info0;
+  uint32_t rb_depth_info;
+  uint32_t rb_modecontrol;
+  uint8_t readback_path;
+  uint8_t pad[3];
+};
+
+struct TrailerRecord {
+  RecordPrefix prefix;
+  uint32_t draws_recorded;
+  uint32_t draws_skipped;
+  uint32_t copies;
+  uint32_t shaders;
+  uint32_t requested;
+  uint32_t started;
+  uint32_t completed;
+  uint32_t dropped;
+  uint32_t truncated;
+  uint32_t overflow_vertex;
+  uint32_t overflow_texture;
+  uint32_t overflow_sampler;
+  int64_t draw_calls_snapshot;
+  uint64_t bytes_written;
+  uint64_t guest_swap_ordinal_close;
+};
+
+#pragma pack(pop)
+
+static_assert(sizeof(RecordPrefix) == 8);
+static_assert(sizeof(HeaderRecord) == 240);
+static_assert(sizeof(ShaderRecord) == 120);
+static_assert(sizeof(VertexBindingRecord) == 24);
+static_assert(sizeof(TextureRecord) == 24);
+static_assert(sizeof(SamplerRecord) == 8);
+static_assert(sizeof(DrawRecord) == 3292);
+static_assert(sizeof(CopyRecord) == 56);
+static_assert(sizeof(TrailerRecord) == 80);
+
+// The command callbacks use process-lifetime atomics. This query is the only
+// normal-path boundary hook; it is one relaxed load at IssueSwap and false
+// until a capture command is issued.
+bool HasPendingRequest();
+
+class Recorder {
+ public:
+  bool IsRecording() const { return recording_ && !capture_truncated_; }
+  bool HasOpenCapture() const { return recording_; }
+
+  void OnSwap(uint32_t title_id, uint64_t guest_swap_ordinal,
+              uint64_t guest_vblank_counter, uint64_t vk_frame_current,
+              uint64_t vk_submission, uint32_t mode_flags, int64_t draw_calls_snapshot);
+
+  bool RecordShader(ShaderRecord record);
+  bool RecordDraw(DrawRecord record);
+  bool RecordCopy(CopyRecord record);
+  void NoteDrawSkipped();
+
+ private:
+  bool Start(uint32_t title_id, uint64_t guest_swap_ordinal,
+             uint64_t guest_vblank_counter, uint64_t vk_frame_current,
+             uint64_t vk_submission, uint32_t mode_flags);
+  void Complete(uint64_t guest_swap_ordinal_close, int64_t draw_calls_snapshot);
+  bool Append(const void* data, size_t size, bool reserve_trailer = true);
+  void MarkTruncated();
+
+  bool recording_ = false;
+  bool capture_truncated_ = false;
+  uint32_t capacity_draws_ = 0;
+  uint32_t event_ordinal_ = 0;
+  uint32_t draws_recorded_ = 0;
+  uint32_t draws_skipped_ = 0;
+  uint32_t copies_ = 0;
+  uint32_t shaders_ = 0;
+  uint32_t overflow_vertex_ = 0;
+  uint32_t overflow_texture_ = 0;
+  uint32_t overflow_sampler_ = 0;
+  uint64_t guest_swap_ordinal_open_ = 0;
+  size_t max_buffer_bytes_ = 0;
+  std::filesystem::path output_path_;
+  std::vector<uint8_t> buffer_;
+  std::vector<std::pair<uint64_t, uint8_t>> shaders_seen_;
+};
+
+}  // namespace rex::graphics::vulkan::draw_census
diff --git a/src/graphics/vulkan/native_residency.cc b/src/graphics/vulkan/native_residency.cc
index 6e172c5..d36d379 100644
--- a/src/graphics/vulkan/native_residency.cc
+++ b/src/graphics/vulkan/native_residency.cc
@@ -176,6 +176,7 @@ void NativeResidency::PrefetchFrame(memory::Memory& memory, SharedMemory& shared
 void NativeResidency::BeginDraw(bool allowed) {
   draw_index_residency_allowed_ = native_index_frame_ && allowed;
   draw_vertex_residency_allowed_ = native_vertex_frame_ && allowed;
+  draw_index_native_ = false;
   draw_used_native_ = false;
   draw_fell_back_ = false;
 }
@@ -461,6 +462,25 @@ NativeResidency::VertexRangeResult NativeResidency::RequestVertexRange(memory::M
              : VertexRangeResult::kFallback;
 }
 
+bool NativeResidency::LastRequestedRangeHash(uint32_t start, uint32_t length,
+                                             uint64_t* out) const {
+  if (!out) {
+    return false;
+  }
+  auto it = range_table_.find(RangeKey(start, length));
+  if (it != range_table_.end() && it->second.hash_valid) {
+    *out = it->second.hash;
+    return true;
+  }
+  it = range_table_.find(ConversionSourceKey(start, length));
+  if (it != range_table_.end() && it->second.hash_valid) {
+    *out = it->second.hash;
+    return true;
+  }
+  *out = 0;
+  return false;
+}
+
 bool NativeResidency::MayFingerprintConversionSource(const void* guest_source, uint32_t start,
                                                      uint32_t length) {
   if (!draw_index_residency_allowed_) {
@@ -643,7 +663,7 @@ bool VulkanPrimitiveProcessor::TryMakeIndexBufferNativelyResident(
   std::memcpy(host_indices, guest_indices, size_bytes);
 
   host_index_buffer_handle_out = handle;
-  residency->NoteDrawNativeUsed();
+  residency->NoteDrawIndexNative();
   PERF_counter_add(kNativeBytesUploaded, size_bytes);
   return true;
 }
diff --git a/src/graphics/vulkan/native_residency.h b/src/graphics/vulkan/native_residency.h
index 89aab50..237e665 100644
--- a/src/graphics/vulkan/native_residency.h
+++ b/src/graphics/vulkan/native_residency.h
@@ -80,11 +80,16 @@ class NativeResidency {
   void BeginDraw(bool allowed);
   bool IsIndexResidencyAllowedForDraw() const { return draw_index_residency_allowed_; }
   bool IsVertexResidencyAllowedForDraw() const { return draw_vertex_residency_allowed_; }
+  bool WasIndexNativeForDraw() const { return draw_index_native_; }
 
   // Per-draw outcome, recorded by both halves of the path and totalled once in
   // EndDraw so that a draw counts as exactly one native draw or one fallback
   // draw however many ranges it referenced.
   void NoteDrawNativeUsed() { draw_used_native_ = true; }
+  void NoteDrawIndexNative() {
+    draw_index_native_ = true;
+    draw_used_native_ = true;
+  }
   void NoteDrawFallback() { draw_fell_back_ = true; }
   void EndDraw();
 
@@ -112,6 +117,11 @@ class NativeResidency {
                                        uint32_t start, uint32_t length,
                                        bool caller_cache_hit = false);
 
+  // Read-only census accessor. It never fingerprints or touches guest memory;
+  // false means the range is unknown or was deliberately kept below
+  // gpu_native_hash_min_bytes.
+  bool LastRequestedRangeHash(uint32_t start, uint32_t length, uint64_t* out) const;
+
   // Makes [start, start + length) readable by the GPU out of the shared memory
   // buffer without arming a write watch for it, by fingerprinting the guest
   // bytes and copying them in when the fingerprint has moved.
@@ -261,6 +271,7 @@ class NativeResidency {
   uint32_t hash_min_bytes_frame_ = 0;
   bool draw_index_residency_allowed_ = false;
   bool draw_vertex_residency_allowed_ = false;
+  bool draw_index_native_ = false;
   bool draw_used_native_ = false;
   bool draw_fell_back_ = false;
 
